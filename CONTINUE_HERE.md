# ЧПУ Система — быстрый старт для продолжения работы

Короткая ориентировка для новой сессии. **Полное техническое описание —
[HANDOVER.md](HANDOVER.md)** (архитектура по каждому app, бизнес-правила,
открытые вопросы, хронология). **Деплой — [DEPLOY.md](DEPLOY.md)**.
Этот файл их не дублирует: только текущий статус и как продолжить.

## Статус на 2026-08-27

- Стек: **Python 3.13.5 + Django 6.0.6**, venv в `.venv/` настроен.
  `python manage.py test` → **834/834 зелёных** (полный прогон ~8 с — под
  `manage.py test` в settings включается MD5-хешер паролей, раньше PBKDF2
  в каждом `setUp` растягивал прогон до ~6,5 мин;
  число проверять прогоном, а не верить этой строке — 17.08 здесь стояло
  541 при реальных 673; 18.08 прогон подтвердил 738, после правок по аудиту
  п. 1–3 — 764, п. 6–7 — 774, п. 8–10 — 793, п. 11/13/14 — 804,
  п. 16–18 — 807; после прогона ТЕСТИРОВАНИЯ.md 27.08 — 834).
- Система развёрнута в проде: https://chpucenter.com (сервер 167.233.170.216,
  Docker + PostgreSQL + nginx на хосте, Cloudflare).

> 🟡 **Боевых данных пока нет, но на проде УЖЕ появляются записи — проверять
> каждый раз, а не верить этому файлу.**
>
> Структурные правки всё ещё дешёвые: менять модели, удалять поля, переписывать
> миграции можно, не выдумывая переносы данных. Но окно закрывается, и «на проде
> пусто» больше не аксиома.
>
> **Проверять перед тем, как ломать или пересоздавать базу** — на проде:
> ```
> docker compose -f docker-compose.prod.yml exec db sh -c 'psql -U "$POSTGRES_USER" -d "$POSTGRES_DB" -c "select (select count(*) from sales_receipt) as чеки, (select count(*) from clients_client) as клиенты;"'
> ```
> локально:
> ```
> python manage.py shell -c "from sales.models import Receipt; from warehouse.models import InventoryLog; print(Receipt.objects.count(), InventoryLog.objects.count())"
> ```
> Не нули — **остановиться и спросить пользователя, что это за записи**. Ответ
> «тестовые, сносите» может дать только он.
>
> **Так уже было дважды, оба раза из-за этого файла.** Одна редакция уверяла,
> что на проде реальные чеки и долги, — в проект уехали три миграции с переносом
> данных, который никогда не понадобился. Следующая уверяла обратное («заказчик
> не вводил ничего»), а в базе на 2026-08-03 нашлись 4 чека и 2 клиента; они
> оказались тестовыми, и том Postgres снесли осознанно. Мораль одна: цифра из
> команды, а не фраза из документации.

> 🧹 **Миграции пересобраны с нуля** — по одной `0001_initial` на приложение
> вместо 42 файлов с переносами и бэкфиллами. Уцелели только справочники, на
> которые опирается код: `finance/0002_builtin_expense_kinds` (строки отчёта),
> `warehouse/0002_builtin_material_types` (Форекс, Акрил, Оргстекло…),
> `warehouse/0003_builtin_production_sites` (Бишкек, Глобал).
>
> **Правило на будущее:** пока данных нет, новую правку модели класть не
> отдельной `0002_add_…`, а пересобирать `0001_initial` заново — иначе снова
> накопится история, которой незачем существовать.
>
> Из-за этого `migrate` на базе от старой схемы падает с
> `relation … does not exist`: Django считает `0001_initial` применённой по
> старой записи и пропускает создание таблиц. **На проде это уже случилось и
> решено** — 2026-08-03 том Postgres пересоздан, база чистая. Если наступите
> снова (другой сервер, откат) — разбор в `DEPLOY.md`, шаг 4, там проверка
> содержимого базы идёт до необратимой команды. Локально то же самое —
> `rm db.sqlite3 && python manage.py migrate && python manage.py seed`.

> ✅ **Прод пересоздан начисто 2026-08-18** (`down -v` + `up -d --build` +
> `seed` + учётки): на нём весь код по `ab8ef1f` (рулон метрами, аудит). Старые
> записи (3 чека, 3 клиента, 11 материалов) были тестовыми и снесены по решению
> пользователя — **с этого момента данные на проде считать боевыми.** Учётки:
> `admin` (админ), `Лазер` и `Чпу` (складовщики — по станкам), `Бухгалтер`
> (бухгалтер); seed-овских `storekeeper`/`accountant` нет. Каталог — 16
> заготовок из `seed` с нулевым остатком, пять демо-расходников с выдуманным
> остатком удалены. `TELEGRAM_WEBHOOK_SECRET` в `.env.prod` пока пуст — вебхук
> клиентского бота закрыт.
>
> Обновление выполняет пользователь, доступа к серверу у ассистента нет:
> `cd /opt/chpucenter && git pull && docker compose -f docker-compose.prod.yml up -d --build`
> (оболочка на сервере — fish; heredoc'и — только из `bash`). Если правился
> `deploy/nginx/chpucenter.com` — его копируют на хост отдельно
> (`DEPLOY.md`, шаг 5), докер конфиг nginx не подхватывает.

## Что важно знать про поведение (недавно менялось)

Эти правила — решения заказчика, не «баги»; не «чинить» обратно:

- **Аудит «где система врёт про деньги и остатки» (2026-08-18)** — отчёт
  таблицей в чате сессии; правки идут по одному пункту, каждая со своим тестом.
  Закрыто:
  - **Дозаказ (п. 1).** Новые строки уходят со склада тогда же, когда ушли
    остальные строки чека — по `_stock_was_deducted`, а не по статусу оплаты:
    наличный заказ в долг (PENDING) списывает дозаказ сразу, как и первую
    продажу; онлайн-счёт без подтверждения ждёт `confirm_payment`. Раньше
    дозаказ в заказ-в-долг не списывался вовсе (лист ушёл с полки, остаток тот
    же, себестоимость 0, а возврат клал на склад два листа вместо одного).
    Доплата по оплаченному заказу — ДОЛГ: чек снова PENDING, `debt` = разница,
    принимают через «Принять оплату» (админ), деньги доходят до кассы. Раньше
    статус оставался PAID, долг 0, «Принять оплату» отвечала «долга нет», и
    доплата исчезала отовсюду. Онлайн-счёт шлюзом не перевыставляется.
    Заодно: `Receipt.recalculate_total()` складывает ВСЕ строки, включая
    возвращённые (`TransactionItem.sold_total`; возврат уменьшает
    `refunded_amount`, а не итог) — иначе любой пересчёт после частичного
    возврата вычитал возврат дважды; а правка/дозаказ чека с возвратом
    считают от денег НА РУКАХ (`_money_held` = min(оплачено, оставшееся)):
    уценка строки после возврата больше не превращает уже выданные деньги в
    сдачу второй раз. Тесты — `sales/tests_add_items_stock_debt.py`.
  - **Способ продажи материала — явный (п. 2).** `SaleItemInputSerializer`:
    у листа/рулона (`is_roll_material`) `mode` ОБЯЗАТЕЛЕН — без него сервер
    раньше молча считал «кв.м», и дозаказ «1 лист» уходил как 1 кв.м по цене за
    квадрат (1 250 вместо 3 700, со склада 1 кв.м вместо 2.98). Рулон
    (`sells_by_metre`) — ТОЛЬКО `METER`: у него нет ни цены за кв.м, ни за
    штуку, и площадь продавала его за 0 сом (так делал «Повторить заказ» —
    METER-строка проваливалась в «материал по площади»). `METER` — только у
    рулона. Резка/монтаж с материалом-рулоном и размерами куска — 400 (материал
    по площади за 0); работа реза по рулону — строкой без размеров куска.
    Штучный материал режима не требует. Дозаказ (`AddToOrderModal`) шлёт
    режим явно: у листа вкладки «Штучный / Квадратный метр» с ценой за лист или
    за кв.м, у рулона — длина в метрах по цене за метр (рулон — FIFO), рулоны
    из списка материалов к резке убраны; «Повторить заказ» собирает METER-строку
    метрами (`material-metre`), строки рулона не метрами — пропускает вслух.
    Тесты — `sales/tests_sale_mode_required.py`; в
    `sales/tests_roll_by_metre.py::test_mode_is_never_guessed_by_the_server`
    ожидание изменено с 201 на 400 (не угадывать ≠ пропускать).
  - **Гашение долга в кассе — из тех же денег (п. 3).** `amount_paid` при
    `pay_debt` — ВСЁ, что клиент принёс: сначала закрывается новый заказ,
    остаток гасит долги от старых к новым (`_settle_old_debts` внутри
    `create_sale`, каждое погашение — обычный `apply_payment` с записью
    `Payment` и приходом в кассу), что не пригодилось — сдача на новом чеке;
    «Вся сумма» — заказ и долги целиком (поле в кассе показывает заказ + долг);
    пустая сумма без «Вся сумма» при наличии долга — 400 («укажите, сколько
    принёс»). С зачётом сдачи (`use_change`) при погашении долга сдача
    зачитывается в заказ ПЕРВОЙ — как и обещает касса («к получению» = заказ −
    сдача + долг). Раньше сумма сверх заказа становилась сдачей, а долги
    закрывались отдельно и целиком: кассир, вписавший «заказ + долг» (как и
    просила подсказка), получал двойной счёт — долг закрыт, у клиента «сдача»
    на ту же сумму, касса записала её дважды, следующий заказ закрывался этой
    сдачей бесплатно. Подсказки в кассе переписаны (`checkout.payDebtHowTo`,
    `prepayWithDebtHint`, `payDebtNeedsAmount`), «Долг/Сдача» под полем считаются
    от заказа и долга вместе. Тесты — `sales/tests_pay_debt_amount.py`.
  - **Отмена накладной убирает её из закупа (п. 6).** `unpost_supply` удаляет
    движения склада по накладной (как `delete_receipt` у чека), а не оставляет
    их встречной корректировкой: раньше логи поступления отвязывались от
    документа (`supply=None`) и `purchases_from_stock_by_day` считал их
    одиночными приходами — 35 000 сом отменённой накладной сидели в «Закуп
    материала» навсегда, складской лист показывал её в «поступлении». След с
    составом — в журнале действий (`supply_summary`). Тесты —
    `warehouse/tests_supply_cancel_purchase.py`.
  - **Списание рулона — по рулону и в метрах (п. 7).** `POST
    /warehouse/rolls/<id>/write-off/` (`write_off_roll`: метры × ширина ЭТОГО
    рулона, себестоимость по его цене за метр, в журнале склада «Рулон №N: 2 м»,
    в журнале действий — сумма списанного). Общее `/materials/write-off/` для
    `sells_by_metre` — 400: оно шло FIFO со старейшего рулона, «порвали 2 м
    рулона №8» обнуляло целый №7 и списывало 171 сом вместо 400. Вкладка
    «Списание» при рулонном материале спрашивает рулон и метры, показывает
    остаток рулона после. Тесты — `warehouse/tests_roll_write_off.py`.
  - **Неявный ноль цены/ставки — отказ, явный — подарок (п. 8).**
    `SaleItemInputSerializer`: если override не прислан, а в каталоге ставки
    резки нет ни у станка, ни у материала (у обоих станков `rate_per_pm = 0`,
    «берётся у материала»), или у материала нет цены за кв.м / лист / пог.м /
    единицу — 400 с указанием, чего не хватает и кому исправить. Явные
    `cut_rate=0` / `material_price=0` — по-прежнему подарок админа. Нулевое
    количество строки материала — 400. Касса шлёт override ТОЛЬКО если админ
    правил поле (флаги `*Edited`; повтор заказа помечает цены, взятые из
    старого чека за отсутствием каталожной); строка «Работа» в окне видна и при
    нулевой ставке — красным, с причиной, кнопка «Добавить» закрыта, пока
    ставку/цену не задали (админ может вписать 0 = подарок); плитка штучного
    материала без цены не нажимается. Дозаказ — те же подсказки. Раньше
    складовщик видел только «Материал 1 250 × 0.5 = 625» и продавал резку
    бесплатно, а лазер без материала уходил за 0 (`tests_edge_01` это даже
    закреплял — переписан). Тесты — `sales/tests_implicit_zero_price.py`.
  - **Строка накладной без суммы не выпадает молча (п. 9).** Сетка
    (`Supplies.jsx`) держит кнопку закрытой, пока в каждой НАЧАТОЙ строке нет
    материала и суммы (0 — если бесплатно, но названный явно), подсвечивает
    строку и перечисляет номера; раньше такая строка просто исключалась
    («Оприходовать (7)» при восьми) и материал не приходил на склад. Сервер:
    `cost` обязателен и ≥ 0. Тесты — `warehouse/tests_supplies.py::SupplyLineCostTests`.
  - **Обрезок и площадь резки — по ширине ПАРТИИ (п. 10).**
    `TransactionItem.roll_width` — ширина рулона строки (партии, при её
    отсутствии — карточки); по ней `offcut_area` и площадь резки в финотчёте
    (рулон 1.5 при карточке 1.2 давал обрезок 0.4 кв.м вместо 1.0). Строка METER
    без выбранного рулона запоминает рулон, с которого FIFO начал. Накладная
    подставляет ширину рулона из карточки (`post_supply` и сетка) и подсвечивает
    ширину, отличную от карточки; касса считает «шире рулона» и обрезок по
    ширине ВЫБРАННОГО рулона. Тесты — `sales/tests_lot_width.py`.
  - **Складской лист считает рулон метрами (п. 11).** `sheet_unit(material)`
    → `METER` у `sells_by_metre` (раньше рулон с размером листа в карточке
    считался ЛИСТАМИ, а метры METER-строк складывались как кв.м: «продано 1 м»
    → «1.000 кв.м / 0.42 листа»). Поступление — по ПАРТИЯМ (площадь ÷ ширина
    партии, `Roll`), продажи — метры METER-строк (SQM-строки рулона — площадь ÷
    ширина партии строки), остаток — `metres_remaining`, колонки по дням — в
    метрах, `counted_in = "METER"`; `sold_area` у рулона справочно, по ширине
    партии. В таблице «Остатки по месяцам» у чисел появилась единица
    (пог.м / лист. / шт) — и в CSV тоже. Тесты — `finance/tests_sheet_roll_units.py`.
  - **Дата проведённой накладной переносится вместе со следом (п. 13).**
    `SupplyViewSet.update` при смене `received_on` зовёт `move_supply_date`:
    партии (`Roll.received_at`, по ним FIFO) и движения склада
    (`InventoryLog.happened_at`, по ним складской лист) уезжают на полдень
    новой даты вместе с закупом; замок периода держит ОБЕ даты (из закрытого
    месяца и в закрытый — 400), правка номера/оплаты/примечания без даты —
    свободно; перенос пишется в журнал действий. Раньше уезжал только закуп, а
    партии и журнал оставались в старом месяце, и никакого замка. Тесты —
    `warehouse/tests_supply_date_move.py`.
  - **Ручная цена/ставка — только админ; закупка складовщику не отдаётся
    (п. 14).** `material_price` / `cut_rate` от не-админа в checkout и add-items
    → 403 «правит только администратор» (раньше API принимал их от кого угодно
    — чек за 0 при себестоимости 4 087). `RollSerializer` отдаёт
    `purchase_cost` / `cost_per_sqm` / `cost_per_pm` только `sees_money`
    (админ, бухгалтер), `MaterialSerializer` — `purchase_price` / `stock_value`
    так же (поле остаётся записываемым для карточки); в кассе подпись рулона у
    складовщика — номер и остаток, без себестоимости метра. Тесты —
    `sales/tests_role_price_guard.py`; в `sales/tests.py`, `tests_edge_01`,
    `tests_edge_02` тесты ручных цен теперь ходят админом.
  - **Техкарта «на кв.м» — от площади куска (п. 16).** `recipe_consumption`
    (`sales/sale_service.py`): норма «на кв.м» × площадь `width × length`
    строки резки (у реза целого листа площади нет — 0), «фикс» — раз на строку;
    та же формула в обзоре (`materials_consumed_by_services`). Раньше норма
    умножалась на `quantity` строки резки — а это ПОГОННЫЕ метры: 0.1 клея на
    кв.м при куске 0.5 кв.м и 8 пог.м реза списывало 0.8 вместо 0.05. Техкарты
    в UI только читаются (заводятся через админку) — дыра спала. Тесты —
    `sales/tests_recipe_area.py`.
  - **«% ЗП мастера» показывается (п. 17).** Финотчёт: `cutting.master_share`
    = % от стоимости работы резки за период (целыми сомами) и по сотрудникам
    (`by_user[].amount`, `master_share`); в блоке «Резка по станкам» строка
    «ЗП мастера по ставке N % от работы резки: X» с пометкой «справочно — в
    прибыль не входит, зарплаты вносятся записями». Раньше настройка
    существовала (`PricingSettings.master_commission_percent`, «Цены и
    услуги»), но нигде не считалась. Тесты — `finance/tests_master_share.py`.
  - **Документация сверена (п. 18):** HANDOVER §10 — два закрытых вопроса
    (долг PARTIALLY_REFUNDED, копейки `line_total`) убраны из «открытых»;
    здесь уточнено, что «заявлено/принято» и цена за метр — только в модалке
    «Поступление»; ТЕСТИРОВАНИЕ.md переписан там, где ожидания расходились с
    кодом (2.7, 3.10, 5.1, 10.1, 1.3 и новые сценарии по пунктам аудита).
- **Рулон продаётся ПОГОННЫМИ МЕТРАМИ, лист — площадью (2026-08-18).** Тема
  доведена целиком по плану заказчика (его формулировка: «половина модели в
  метрах, половина в квадратах хуже, чем любая из двух целиком»):
  - **Развилку решает СПРАВОЧНИК, не касса.** `Material.sells_by_metre` =
    `is_roll_material and intake_form == ROLL` — и только. У рулона в кассе
    ОДНО поле — длина; ширина надписью; вкладок нет. Режим `METER` касса шлёт
    ЯВНО, сервер по справочнику его НЕ угадывает (иначе 1.26 кв.м молча стали
    бы 1.26 пог.м). Ширина в развилку не входит: раньше рулон с пустой
    шириной молча откатывался к четырём вкладкам и площади — теперь ширина у
    формы «Рулон» **обязательна** (`MaterialSerializer.validate`, красная
    подсказка в форме), а продажа метрами от неё не зависит. Тесты —
    `warehouse/tests_roll_width_required.py`, `sales/tests_roll_by_metre.py`.
  - **Ширина заморожена в ПАРТИИ** (`Roll.width`), метры считаются по каждому
    рулону его шириной (`consume_metres` / `restore_metres`,
    `Material.metres_remaining` — сумма по рулонам). `Material.roll_width` —
    лишь значение по умолчанию для приёмки; правка опечатки в карточке не
    двигает принятые рулоны. Под одной карточкой законно лежат рулоны разной
    ширины. Тесты — `warehouse/tests_roll_lots.py`.
  - **Приход рулона — в метрах и по цене за метр** (`cost_per_pm`), пара
    «заявлено / принято» (`Roll.declared_length`), недолив уходит в журнал
    действий. Себестоимость метра — `Roll.cost_per_pm`. **Только в модалке
    «Поступление» на строке материала (`receive-roll`, админ).** Приходная
    НАКЛАДНАЯ — единственный путь складовщика — принимает рулон как ширина ×
    длина + сумма строки: ни «заявлено», ни цены за метр там нет (аудит
    18.08, п. 18; ширина с 18.08 подставляется из карточки). Если нужна пара
    «заявлено/принято» у складовщика — это поле `SupplyLine.declared_length`
    и колонка в сетке, отдельной правкой.
  - **Партия = физический рулон.** В кассе выбирают, из какого рулона режут
    (початый первым — «дожечь»), строка чека помнит рулон
    (`TransactionItem.roll`, «списано с рулона №7»), возврат идёт в ТОТ ЖЕ
    рулон. Тесты — `sales/tests_roll_choice.py`.
  - **Инвентаризация рулона — ТОЛЬКО промером по рулону и в метрах:** Склад →
    «Промер №N» → `POST /warehouse/rolls/<id>/stocktake/` → акт
    `RollStocktake` (было / намерено / расхождение числом / причина; акт
    read-only, «Прочее» без пояснения не проходит). Общая правка остатка в
    кв.м (`/materials/adjust/`, кнопка «Остаток») у такого материала
    **закрыта на сервере (400)** и убрана из интерфейса — она гнала
    расхождение через FIFO, то есть списывала полтора метра со СТАРЕЙШЕГО
    рулона вместо промеренного. Единственное, что у него правится числом, —
    «хвост сверх партий» (кв.м, не принадлежащие ни одному рулону: материал
    переключили на рулон уже с остатком и т.п.): кнопка «Свести с рулонами»
    появляется только когда остаток ≠ сумме рулонов и зовёт
    `POST /materials/reconcile-lots/` (число := сумма партий, разница
    строкой ADJUSTMENT в журнал). Тесты — `warehouse/tests_roll_stocktake.py`,
    `warehouse/tests_adjust_roll_guard.py`.
- **Резка без длины реза НЕ проходит (2026-08-18).** Длину кривой у фигурного
  реза вводит мастер; пустое поле раньше молча уезжало в чек нулём — материал
  посчитан, самая дорогая работа цеха бесплатно (в CONTINUE это даже стояло
  правилом: «без них не начисляет работу»). Теперь пустая/нулевая длина у
  услуги резки — 400 в `SaleItemInputSerializer` (касса и дозаказ), в кассе
  кнопка «Добавить» закрыта с красной причиной, строка из повтора старого
  заказа без длины помечена в корзине. Нулевая СТАВКА (`cut_rate=0`) —
  по-прежнему законный подарок админа. Площадь вместо длины не подставляется
  никогда. Тесты — `sales/tests_cut_requires_length.py`.
- **Ревизия интерфейса глазами заказчика (2026-08-16)** — отчёт со
  скриншотами в `ui-review/REPORT.md`; пункты 1–22 из него починены
  16–17.08 (23 — не дефект приложения):
  - **Печать шла пустым листом.** Окна печати (`PrintDocs`, `PrintSupply`,
    `PrintAct`) рендерились внутри `.shell`, а `@media print` прячет `.shell`
    целиком. Теперь они выносятся порталом в `<body>` (`components/PrintHost.jsx`),
    в печати спрятан весь `#root`. Проверять печать надо PDF-ом
    (`page.pdf()` в Playwright или «Сохранить как PDF»), а не предпросмотром —
    предпросмотр и был красивым.
  - **Площадь куска — одно число у кассы и сервера.** Она квантуется до
    0.001 «половиной вверх» ДО расчёта цены и списания
    (`sale_service._area/_qty`; так хранит колонка `quantity`, так же округляет
    Postgres), касса считает так же (`utils/area.js`, целочисленно по десятичной
    записи ввода — `toFixed(3)` резал 0.5535 до 0.553). **«Вся сумма» шлёт флаг
    `pay_full`, а не число**: сумму чека знает только сервер, касса её не
    угадывает. Поле `quantity` в checkout принимает три знака (было два, и
    продажа площади 0.554 кв.м отклонялась). Тесты — `sales/tests_rounding.py`.
  - **Акт сверки видит возвраты.** Строка «Возврат по заказу №N» (погашение)
    и «Выдано деньгами по возврату №N» (начисление, не больше принятого) —
    датой заказа, как возвраты относит к заказу вся отчётность. Сальдо акта =
    долг в карточке. Заодно `refund_receipt` считает возврат по `line_total`
    (вверх до сома), а не сырым `qty × price`.
  - **График «По дням» видит закуп по приходам** (`purchases_from_stock_by_day`)
    — те же условия, что у строки «Закуп материала» в плитках (вид не в архиве
    и «уменьшает прибыль»). Плитки и график показывают одну прибыль. Тесты —
    `finance/tests_daily_purchases.py`, `finance/tests.py::CogsTests`.
  - **Правка чека шлёт и пишет в журнал только то, что изменилось.** Форма
    отправляла все три поля разом, и любое сохранение (даже правка одного
    количества) переставляло время заказа на полдень — чек и его списание
    уезжали в хронологии, а журнал действий писал «client, order_date, title»
    на пустом месте. Тот же день дату не трогает (`_edit_meta`), фронт шлёт
    только изменённые поля. Тесты — `sales/tests_receipt_edit.py`.
  - **Возврат — отдельными позициями и из админских «Чеков».** Общая модалка
    `RefundModal` (кнопка «Возврат» в действиях чека у админа, «Оформить
    возврат» в складской детали): отмечают, что вернули (по умолчанию всё),
    уходит `item_ids`. Два правила бэкенда стали честными: (а) долг считается и
    у частично возвращённого чека (`Receipt.OWING_STATUSES` — PENDING и
    PARTIALLY_REFUNDED — в свойстве `debt`, аннотациях списков, статистике,
    финотчёте и списке клиентов; раньше после возврата одной строки остаток
    долга пропадал отовсюду); (б) деньгами по возврату отдаётся ровно
    переплата относительно ОСТАВШИХСЯ у клиента позиций —
    `max(оплачено − (сумма − возвращено), 0)` до и после возврата, разница в
    кассу (прежнее `min(возврат, оплачено)` при частичной оплате и двух
    возвратах подряд отдавало больше принятого). Так же считают модалка и акт
    сверки. Тесты — `sales/tests_partial_refund.py`.
  - **Складовщик заводит приход сам** — пункт «Приём товара» в его меню
    (`/app/supplies`, тот же экран `Supplies`, что вкладка «Приходы» у админа;
    отмена накладной по-прежнему только админу). Бэкенд это разрешал давно,
    экрана не было.
  - **Шапка модалки** стоит вплотную к верху окна: у `.modal` нет верхнего
    отступа, он у `.modal-head`. Sticky-элемент не может подняться выше
    content-box контейнера, и с `padding-top: 24px` + `margin-top: -24px` шапка
    висела на 24px ниже: контент просвечивал над ней и она накрывала первую
    строку формы. Не возвращать отступ окну.
  - **Таблица «Чеки» — 10 колонок и влезает в 1440** (было 14 и 1650px):
    «кто оформил» под клиентом, способ оплаты под статусом, себестоимость под
    маржой, сдача в колонке долга, все действия одной ячейкой 2×2, плотный
    режим `.table-wrap.dense`. У прокручиваемых таблиц полоса прокрутки
    нарисована явно (на macOS системная видна только во время прокрутки).
  - **Цена листа в Складе = цене в кассе:** розничная строка «сом/лист» — это
    `piece_price` с округлением вверх до сома, как продаёт касса; нет цены за
    лист — нет строки. Раньше склад считал `кв.м × площадь` и показывал 3725
    там, где касса продавала за 3700.
  - **Обзор складывается.** Разбивка «работа / материал» и «покупки по
    клиентам» — по `line_total` (вверх до сома) и в Python: `CEIL` в SQLite
    ловит тот же шум double (0.554×1500 = 831.0000000000001 → 832), что и
    касса. «Выручка всего» — за вычетом возвращённых строк, как в Финансах.
    «Покупки по клиентам» — та же база/период, что «Продали материала на»
    (`/api/audit/client-purchases/?date_from&date_to`). Себестоимость проданного
    — справочная строка под итогом расходов, не в списке. Плитка «на исходе»
    считает как каталог: 0 < остаток ≤ порог; нули — `out_of_stock_count`.
  - **Словари EN/KY полные** (по набору ключей совпадают с RU; проверять:
    `node -e` со сравнением плоских ключей — см. `ui-review/REPORT.md`, п.13).
    Печатные формы собираются на языке интерфейса: заголовок — шаблон
    `print.docHead`, единицы — кодом с сервера (`unit_code` в
    `TransactionItemSerializer` и `SupplyLineSerializer` → `unit.*`), валюта/ИНН/тел.
    — `print.currency/inn/tel`, сумма прописью — `amountInWords(value, lang)`
    (ru/en/ky). Кыргызские тексты писал не носитель — при случае вычитать.
    Не переведено сознательно: слово «сом» в суммах на экранах (в KY то же
    слово), названия видов расхода/счетов/станков из бэкенда.
  - **Мелочи 14–22 из отчёта закрыты** (17.08): карточка клиента показывает
    возвращённые строки зачёркнутыми и «возвращено на N», «Заказов» без
    отменённых, как список (`stats.cancelled_count` отдельно); ввод
    поставщика в накладной при совпадении с существующим выбирает его
    (`RefSelect`), сервер проверяет дубль без регистра кириллицы в Python
    (`SupplierSerializer.validate_name`); окно чека после оформления и
    складская деталь — с единицами, ценами, «Оплачено/Сдача/Долг»; форматы
    чисел (критический остаток «2», журнал «×5 (14.88 кв.м), 12 000 сом»);
    мобильный — чек первым (`.pos-cart { order: -1 }` до 880px), плитки по
    две, «Тип» не режется; плитки «Чеков» пять в ряд; календари парой; акт
    отсортирован; `seed` задаёт лист 1.22×2.44 и приход на «Лист» берёт размер
    с материала (`post_supply`); под выключенным «Счётом на оплату» видимая
    причина; `<html lang>` = язык интерфейса.
- **Погонный метр («длина кривой») зависит от режима реза.** У фигурного реза
  его вводит мастер; у обычного реза в работу идёт **одна сторона куска — поле
  «Длина»** (2026-08-14, просьба заказчика). Площадь вместо длины НЕ
  подставляется никогда: кв.м считались как пог.м, и работа выходила примерно
  втрое дешевле. Бэкенд берёт `running_meters` из запроса — сторону считает
  касса; **без них резка с 2026-08-18 отклоняется, а не начисляется нулём**
  (см. выше). То же и в дозаказе (Чеки → чек → «+ Дозаказать»).
- **Период закрывается на замок (2026-08-14).** Финансы → «Закрытие периода»:
  дата, по которую всё становится только на чтение. Закрывает и открывает
  ТОЛЬКО админ, оба действия идут в журнал — если цифры прошлого месяца
  всё-таки поехали, видно, кто снял замок. Держит: заказы (создание задним
  числом, правка, удаление, возврат, откат оплаты, выдача сдачи), оплаты и
  общие выплаты, траты, кассовые записи, приходные накладные (и их отмену).
  Не держит инвентаризацию и списание — они двигают только текущий остаток и в
  прибыль периода не входят. Проверка одна на всю систему —
  `finance/periods.py::ensure_open`, ошибка приходит обычным 400 с человеческим
  текстом. Тесты — `finance/tests_period.py`.
- **Появилась КАССА (2026-08-14)** — раздел «Касса» (админ ведёт, бухгалтер
  смотрит, складовщику закрыт, как и финотчёт). Отвечает на «сколько сейчас
  должно быть в ящике»: раньше в системе были только обороты — выручка, расходы,
  долги, — а остатка денег не было нигде. Два счёта: наличные и банк (MBank и
  DemirBank — переводы, в ящике их нет).
  **В кассу пишется то, что клиент ПРИНЁС, а не то, что зачлось за заказ:**
  заказ на 36, дал 100 — в ящике 100, и 64 уйдут расходом при выдаче сдачи.
  Оплаты, сдачу, возвраты и откат оплаты система пишет сама (`finance/cash.py`,
  вызовы из `sale_service`), руками вносят закуп за нал, зарплату, инкассацию.
  Автозаписи не правятся и не удаляются — они отражают чеки. Пересчёт кассы
  («в ящике столько-то») пишет разницу отдельной строкой, как инвентаризация
  склада: недостача не затирается. Тесты — `finance/tests_cash.py`.
- **Приход вводится НАКЛАДНОЙ — одним документом (2026-08-14).** Склад →
  «Приходы»: номер бумажной накладной, поставщик, дата и все позиции сеткой.
  Сумма системы стоит рядом с суммой по бумаге, расхождение видно сразу — ради
  этой цифры документ и заведён. Раньше приход вводился ПО ОДНОЙ ПОЗИЦИИ с
  кнопки на строке материала, и сверить итог с накладной было нечем.
  Проводится сразу при создании, теми же примитивами (`receive_lot` /
  `apply_stock_change`), поэтому закуп в финотчёте, FIFO и журнал работают без
  правок. Отменить накладную может только админ и только пока из её партий не
  резали — иначе поехала бы себестоимость закрытых заказов. Появился
  **справочник поставщиков** и долг по каждой накладной (`warehouse/0005`).
  Накладная печатается со сверкой и подписями «Сдал / Принял».
  Кнопка «Поступление» на строке материала осталась: одна банка клея документа
  не заслуживает. Тесты — `warehouse/tests_supplies.py`.
- **Печатные формы есть у заказа и у клиента (2026-08-14).** По заказу —
  товарный чек, накладная и счёт на оплату (кнопка «Печать» в чеках, доступна и
  складовщику); по клиенту — акт сверки. Печатает БРАУЗЕР (`window.print`),
  серверной генерации PDF нет: то же окно печати умеет «Сохранить как PDF», а
  предпросмотр свёрстан листом А4 — что видно, то и уйдёт на бумагу
  (`@media print` в `index.css` прячет всё, кроме `.print-sheet`).
  Шапка и подписи берутся из **реквизитов организации**
  (`finance.CompanyProfile`, Финансы → «Реквизиты для документов»): читать их
  может любой сотрудник — накладную выдаёт складовщик, — менять только админ.
  Счёт не даётся, пока не заполнены банк и расчётный счёт (`has_bank`).
  Сумма прописью — `frontend/src/utils/amountInWords.js`.
- **«Удалить» на материале удаляет НАСОВСЕМ, если по нему не было продаж** —
  вместе с партиями и складским журналом, поэтому приход уходит и из закупа
  (2026-08-14). Раньше граница шла по любой истории: материал прятался уже
  из-за одного прихода, а это самый частый случай («завёл, принял поставку,
  увидел дубль»), — из каталога он пропадал, но оставался в стоимости склада,
  в закупе и в отчёте по материалам. Продажи были — только прячем
  (`is_archived`): строки старых чеков на него ссылаются. Скрытый материал не
  считается в стоимости склада и в «на исходе», а в отчёте по материалам
  показывается только там, где в периоде реально были движения. Проверено
  `warehouse/tests_delete.py`.
- **Контент занимает всю ширину экрана**, не колонку в 1200px по центру
  (2026-08-14, просьба заказчика). На его мониторе слева оставалась пустая
  полоса в четверть экрана, а таблицы чеков и складского листа в 1200px не
  влезали и уезжали под правый край. Ряды плиток — `auto-fit`, а не
  `auto-fill`, иначе пустые колонки оставались справа.
- **Модальное окно закрывается только крестиком** (и «Отменой», если она есть в
  футере) — щелчок по затемнению не закрывает (2026-08-14, просьба заказчика).
  Формы длинные (размеры, цены, ставка реза), и промах мимо окна стирал всё
  введённое без предупреждения. Шапка окна липкая, поэтому крестик виден и в
  середине прокрученной формы.
- **Пустая сумма оплаты = ничего не приняли, весь заказ в долг.** Для обычной
  продажи в кассе есть кнопка «Вся сумма».
- **Переплата запоминается СДАЧЕЙ, а не отбрасывается.** Заказ на 1500, принесли
  3000, а мелочи в кассе не было — 1500 остались у цеха, и это его долг перед
  клиентом, зеркальный обычному долгу. Раньше здесь стоял `min(paid, total)`, и
  назавтра вспомнить, сколько за кем осталось, было нечем.
  Хранится полем `Receipt.change_due`, а НЕ через `amount_paid > total`:
  `amount_paid` участвует в долге, статусе оплаты и во всех отчётах, и
  «оплачено 3000» по заказу на 1500 сделало бы выручку неверной.
  Видно: плитка «Сдача клиентам» и колонка «Сдача» в Чеках, фильтр «Только со
  сдачей» там же и в Клиентах, строка в карточке клиента. Отдаётся кнопкой
  «Выдать» — можно частями (мелочи может не хватить и во второй раз). Откат
  оплаты сдачу снимает: денег не брали — и сдачи с них нет.
- **Состав чека правится: количество, цена, лишняя строка.** Ошибаются не только
  в названии — лишний лист, лишний квадратный метр, цена не та. Строка правится
  ЧЕРЕЗ СКЛАД, а не арифметикой по полям: возвращаем списанное, применяем
  правку, списываем заново. Себестоимость пересчитывается сама (рулонные — по
  FIFO), остаток не расходится с журналом. В журнале склада остаётся ОДНА запись
  продажи с исправленным количеством: техническая пара «возврат + продажа»
  убирается, потому что возврат — это когда клиент принёс заказ обратно, а не
  когда исправили опечатку. Если итог упал ниже уже принятых денег, разница
  становится СДАЧЕЙ. Только админ, каждая правка — в журнале действий с составом
  «было → стало».
- **Общая выплата — Клиенты → карточка → «Погасить долг».** Одна сумма гасит
  долги нескольких заказов сразу, от старых к новым; заказы можно выбрать
  галочками, пустая сумма закрывает выбранные целиком, переплата — сдача.
  Список заказов там БЕЗ фильтра периода: долг у карточки «на сейчас», и
  показывать под него только заказы выбранного месяца было бы враньём.
- **Заказ оформляется задним числом** — поле «Дата заказа» в кассе, ТОЛЬКО у
  админа. Закрыто в трёх местах: поля нет в интерфейсе, checkout отвечает 403,
  и в Django-админке `created_at` доступен на правку только суперпользователю
  (поле перестало быть `auto_now_add`, то есть само по себе стало редактируемым
  — вторая дверь к той же дате). `Receipt.created_at`
  больше не `auto_now_add`, а `default=timezone.now`. Дата опорная для ВСЕЙ
  отчётности: выручка, прибыль по дням, себестоимость проданного, складской
  лист, период в карточке клиента — всё считается по ней. Той же датой пишется
  списание материала в журнале склада (`InventoryLog.happened_at`), иначе расход
  стоял бы сегодняшним днём по заказу за прошлый месяц. Дата хранится полднем
  местного времени: полночь в Бишкеке — это вчера по UTC, и заказ уезжал бы в
  соседний день. **Партии FIFO при этом НЕ отматываются назад** — списываем из
  того, что лежит на складе сейчас, а не из того, что лежало на ту дату.
  Номер заказа остаётся регистрационным: задним числом заказ получает следующий
  свободный номер, а не встаёт в хронологию.
- **Каждая оплата — запись `sales.Payment`** (сумма, способ, дата, кто принял).
  Дату можно поставить задним числом (будущую — нельзя, этих денег ещё нет),
  кривая дата отклоняется, а не подменяется сегодняшней. Откат оплаты удаляет
  записи. Выручка при этом по-прежнему относится к ДАТЕ ЗАКАЗА — запись оплаты
  справочная, отчёты она не двигает. Видно в карточке клиента, блок «Оплаты».
- **Себестоимость и маржа заказа видны в «Чеках» — админу и бухгалтеру.**
  Складовщик оформляет и выдаёт заказы, но закупочных цен не видит (в API
  `cost_total` и `margin` приходят `null`). Маржа = итог − возвраты −
  себестоимость.
- **Список чеков фильтруется по клиенту и по датам заказа** (`?client=`,
  `?date_from=&date_to=`), плюс прежние способ оплаты и статус. Плитки сверху
  считаются по ТЕМ ЖЕ фильтрам, иначе «Долг» показывал бы общий долг цеха под
  списком одного клиента. Период — по дате ЗАКАЗА, той же, по которой считается
  вся отчётность.
- **Ошибочный чек удаляется целиком, а безопасные поля правятся.** Удаление
  (только админ): материал возвращается на склад, оплаты снимаются, движения по
  чеку уходят из складского журнала — заказа не было вовсе. Это НЕ возврат:
  возврат — событие бизнес-жизни (клиент принёс обратно), он обязан остаться в
  отчётах; удаление — исправление опечатки. След остаётся в журнале ДЕЙСТВИЙ,
  вместе с составом удалённого чека. Правка (`PATCH`) — только наименование,
  клиент и дата заказа; при смене даты за ней двигается и списание материала.
  Состав чека правится отдельным действием (`edit-items`, см. выше) — через
  `PATCH` деньги не двигаются.
  > До этого `PATCH` и `DELETE` были открыты ЛЮБОМУ авторизованному и без
  > проверок — складовщик мог переписать `total_price` любым числом, а DELETE
  > стирал чек, не возвращая материал. Кнопок не было, поэтому не замечали.
- **Резка считается по СТАНКАМ: ЧПУ и лазер.** Две услуги резки, у каждой своё
  поле `machine`. В кассе станок выбирается переключателем в окне «Отрезать
  кусок» (показывается, только когда станков правда больше одного), в дозаказе —
  выбором услуги. Ставка: своя у станка (`rate_per_pm`), а если она 0 — ставка
  МАТЕРИАЛА, как было до разделения. Станок впереди материала специально: иначе
  выбор «ЧПУ / лазер» не менял бы цену и читался бы как поломка.
  Блок Финансов называется «Резка по станкам», строки — станки; «Резка, всего»
  осталась. Резка В РАЗРЕЗЕ МАТЕРИАЛОВ никуда не делась — она в складском листе,
  колонка «выручка с резки».
  **Главная величина блока — КВАДРАТНЫЕ МЕТРЫ, а не сумма** (сумма и пог.м ушли
  под цифру): «сколько наработал станок» — вопрос про объём работы, а деньги с
  него следствие ставки. Строки сортируются по площади.
  **«Кто сколько отрезал»** — та же площадь в разрезе людей. Считается по тому,
  КТО ОФОРМИЛ ЗАКАЗ: отдельного поля «мастер за станком» в системе нет, и так
  прямо написано под заголовком — выдавать одно за другое нельзя. Понадобится
  именно резчик — это поле в кассе, вводить на каждый рез.
  > Была ошибка: доля площади считалась как «пог.м строки / пог.м чека», а
  > погонные метры вводятся руками и могут быть не введены. Тогда доля = 0, и
  > площадь попадала в «Резка, всего», но ни в одну строку станка — «всего
  > 1,56 кв.м», под ним «ЧПУ 0 кв.м». Теперь без метров площадь делится поровну
  > между строками резки чека.
- **Роль бухгалтера (`ACCOUNTANT`) — проверяющий, а не участник.** Ему открыты
  журнал действий, финансовый отчёт, обзор и чеки с себестоимостью и маржой,
  всё только на просмотр. Оформлять продажи, принимать деньги, править склад и
  заводить клиентов он не может (`IsNotAccountant` на кассе и клиентах,
  `IsAdminOrAccountantRead` на деньгах). Свой раздел `/acc`, в меню только три
  пункта. Финансовый пароль ему нужен так же, как админу.
- **Цена материала вводится и за лист, и за кв.м.** Заполняешь любое поле —
  второе считается по площади листа. Закупка — одно значение в базе (за кв.м),
  второе поле просто пересчитывает. Розничная — ДВА разных поля (`price_per_sqm`
  и `piece_price`), и цена листа пересчитывается только пока она «в паре»:
  вписал своё число — связь рвётся, ручное значение больше не затирается
  (лист целиком может продаваться дешевле). В таблице склада у ОБЕИХ цен теперь
  стоит единица: у закупочной её не было вообще, и «980 сом» рядом с «1470
  сом/кв.м» выглядели сравнимыми, хотя сравнивать их без единицы нельзя.
- **Кнопки «Изменить» и «Удалить» у материала подписаны, а не спрятаны.** Они
  были безымянными иконками 17px под большой кнопкой «Поступление» — заказчик их
  не нашёл и сообщил, что материал вообще нельзя ни поправить, ни удалить.
  Функция была; не было видно, что она есть. Урок общий: в строке таблицы
  действие подписывают словом (`.row-btn`), иконки без текста не находят.
- **«Резка по станкам» показывает объём работы двумя величинами**, не только
  сумму: 12 000 сом — это много мелких резов или один большой лист, по сумме не
  видно. Под суммой стоит «N пог.м реза · M кв.м материала».
  **Пог.м** — длина кривой (бывшая «длина реза»), количество строки услуги; из неё складывается сумма
  (пог.м × ставка), поэтому она эту сумму и объясняет.
  **Кв.м** — площадь резаного куска, берётся у МАТЕРИАЛА того же чека (продажа
  по кв.м — прямо количество, продажа листами — через площадь листа, штучный
  материал площади не имеет и не считается).
  Это РАЗНЫЕ величины, складывать их между собой нельзя.
- **В «Обзоре» материал показан формулой, а не одной цифрой:** продали −
  себестоимость проданного = прибыль с материала. Плитка «Выручка: материал»
  ничего не объясняла — «продали на 149 232» не отвечает, сколько на этом
  заработали. Себестоимость берётся с ТЕХ ЖЕ строк, что и выручка (тот же
  период, только оплаченные и невозвращённые), иначе формула разъезжается.
  Прибыль тут ВАЛОВАЯ, до аренды и зарплат; итоговую прибыль по-прежнему
  считают Финансы блоком «Материалы» — это разные цифры, и так задумано.
- **Выручка с материала — в складском листе** (Склад → «Остатки по месяцам»),
  группа колонок «Деньги»: выручка с материала · с резки · всего. Те же цифры
  считает `/api/finance/material-report/`, они давно были в API и в свёрнутой
  таблице Финансов — в лист их вынесли, чтобы деньги стояли рядом с
  количествами, как в Excel заказчика.
- **Прибыль = выручка − (Материалы + Постоянные + Переменные), где блок
  «Материалы» = СЕБЕСТОИМОСТЬ ПРОДАННОГО + транспорт + свои строки
  (2026-08-24, решение заказчика).** Закуп — НЕ расход, а оборот: карточка
  «Склад (оборот)» показывает закуп за период и стоимость склада сейчас.
  Повод: ввод каталога на ~700 т. давал «−500 000 прибыли» при пустых
  продажах. Инвестиции (оборудование, улучшение цеха) — ОТДЕЛЬНЫЙ блок, ни в
  прибыли, ни в «Расходах»; строка закупа в «Материалах» справочная.
  **График «По дням» живёт по ТОМУ ЖЕ правилу** (себестоимость по дням, закуп
  из дневных расходов убран). График — второй ВЗГЛЯД на те же деньги, а не
  второй ответ.
- **Прибыль с материала показана формулой прямо на плитке** («продали X −
  себестоимость Y»). Отдельный блок с той же формулой ниже остался, но плитка
  стоит на первом экране, а блок уходит под сгиб — и цифра читалась как «просто
  сумма продажи», непонятно откуда взявшаяся.
- **Трата заводится в диалоге строки отчёта, и только там.** Внизу раздела
  осталась ОДНА таблица «Все траты за период» — вся история месяца одним
  списком, с правкой и удалением строк. Раньше там было три секции
  («Постоянные», «Зарплаты», «Покупки»), у каждой свой ряд плиток с суммами по
  видам и своя форма добавления. Плитки повторяли строки блоков отчёта выше, и
  страница заканчивалась тремя экранами нулей подряд; форма повторяла диалог,
  только хуже — вид расхода приходилось выбирать руками, и не было видно, в
  какую строку отчёта трата попадёт. Одно действие в двух местах — два места,
  где можно ошибиться.
- **Все расходы — записи с датами**, их нужно вносить каждый месяц; вечной
  суммы в настройках больше нет. Вносятся в диалоге своей строки отчёта.
- **Виды расхода заводит админ.** Строки блоков — справочник `ExpenseKind`, а не
  перечисление в коде. У вида есть галка «уменьшает прибыль»: снятая означает
  вложение (как покупка оборудования) — видно в отчёте, прибыль не трогает.
- **Все три блока устроены одинаково:** строка = вид расхода, клик открывает
  список трат, внизу «+ вид расхода». Закуп, транспорт и долг материала — такие
  же виды с записями (раньше были одним числом в настройках).
- **Остатков материала в отчёте нет (2026-08-14), а с 2026-08-24 из прибыли
  ушёл и закуп** — расход материала теперь себестоимость проданного (см. выше).
  Колонки остатка из складского листа убраны. Прибыль от этих правок менялась
  осознанно.
- **Выручка — по всем заказам периода**, включая отданные в долг (минус
  возвраты, кроме отменённых). Рядом видно, сколько уже на руках и сколько ещё
  должны. Раньше заказ в долг в выручку не попадал, и месяц выглядел пустым;
  вдобавок «Обзор», «Финансы» и график по дням считали её тремя разными
  способами.
- **Отчёт считается за выбранный месяц** (селектор сверху раздела, по умолчанию
  текущий). Раньше блоки считались за всё время.
- **Складской лист — Склад → «Остатки по месяцам»**: поступление, проданные,
  производство и деньги по каждому материалу. Остатки на начало и на конец из
  него убраны; API их по-прежнему считает, вернуть колонки — правка одного
  компонента (`MaterialStock.jsx`).
- **Номенклатура разобрана на поля.** У материала есть `type` (справочник
  `MaterialType`), `thickness_mm`, `color`, `article`, `sheet_width`,
  `sheet_height`, `production` (справочник `ProductionSite`). Свободного текста
  `category` нет. Площадь листа считается из размера, название собирается из
  полей и остаётся редактируемым — привычные подписи вроде «синий бишкек» у
  заказчика не отнимаем.
- **Площади хранятся с четырьмя знаками.** При двух `1.22 × 2.44 = 2.9768`
  округлялось до `2.98`, и целые листы превращались в дробь в той самой
  колонке, которую он сверяет с Excel. Не понижать обратно.
- **Остаток рулонного материала хранится ДВАЖДЫ и обязан сходиться:** числом в
  `Material.quantity` и площадями партий `Roll.remaining_area`. Любая операция,
  двигающая склад, должна двигать обе записи — для рулонных это `consume_area` /
  `restore_area` (площадь), `consume_metres` / `restore_metres` (метры, по
  рулонам), `stocktake_roll` (промер), а НЕ `apply_stock_change` (он знает
  только про число). На этом уже попались инвентаризация и списание брака в
  `seed_demo`. Разошлись у материала, продающегося метрами, — «хвост сверх
  партий», сводится только `reconcile_with_lots` (см. выше про промер).
  Проверяется `sales/tests_stock_integrity.py` после каждого пути и в
  комбинациях; при новой операции со складом добавлять туда же.
- **«Оплачено полностью» — флаг `pay_full`, а не большое число.** Сумма чека
  известна только внутри `create_sale`, и вызывающий её заранее назвать не
  может. Раньше передавали `9 999 999` и полагались на обрезку до суммы чека;
  с тех пор как переплата запоминается сдачей, такой сентинел превращается в
  девять миллионов сдачи клиенту. Намерение называть, а не кодировать числом.
- **Дата прихода редактируемая** (`InventoryLog.happened_at`,
  `Roll.received_at`). Заказчик вносит поставки задним числом — в его таблице
  даты идут вразнобой. По этой же дате строится FIFO.
- **Руками вводится только то, чего система знать не может.** Это главный
  принцип: заказчик пришёл из Excel, где вбивал всё. Теперь считается само —
  закуп материала (по приходам на склад), стоимость склада (по остаткам
  партий), длина обычного реза (одна сторона куска), форма прихода (с
  материала). **Любую цифру можно перебить вручную**, ручное значение побеждает
  расчёт.
- **Клиент опознаётся по ЦИФРАМ телефона, а не по строке.** Номер хранится так,
  как его набрали (заказчику привычнее), но ищется через `clients/phones.py` —
  по последним девяти цифрам, с приоритетом точного совпадения. Раньше касса
  сравнивала строки, и `0555 111 222` заводил второго клиента поверх
  `+996555111222`: один человек становился двумя карточками, его заказы и долг
  расходились надвое («Тахир ака» и «ака Тахир» — разные люди). Тем же поиском
  пользуются заведение клиента руками (там теперь понятная ошибка «номер уже
  записан за …») и вход в клиентский кабинет.
  **Уже накопившиеся двойники этим не склеиваются** — их показывает
  `manage.py find_duplicate_clients` (только смотрит, ничего не меняет), а
  склеивает кнопка в карточке клиента: Клиенты → карточка → «Объединить с
  другой карточкой». Только админ, с предпросмотром («переедет заказов N, долг
  X») и подтверждением — вторая карточка удаляется безвозвратно. Переносится
  ВСЁ, что на неё ссылалось: заказы, оплаты, рефералы и заявки на смену
  реферера (`new_referred_by` стоит на CASCADE — не перенести её значит потерять
  заявки вместе с карточкой). Логика — `clients/merge.py`.
- **Пароль клиентского портала выдаёт админ.** Клиент себе пароль не заводит,
  складовщик выдать не может (403). Одноразовый код входа удалён.
- **Оплата долга и откат оплаты — только админ.**
- **Товар с историей продаж не удаляется, а скрывается** (`is_archived`).
- **Пустое числовое поле в форме материала — это `""`, а не ноль.** Сервер на
  `""` отвечает «Требуется численное значение» и заворачивает весь материал.
  Ловилось так: открыть материал, стереть цену, чтобы вписать заново, сохранить.
  Чинится в `Catalog.jsx`: `withNumbersFixed` переводит пустые цены и остатки в
  `0`, а толщину и размеры листа (они `null=True`) — в `null`. **У `save()`
  теперь есть `catch` с тостом** — раньше его не было вообще, промис падал
  молча, модалка оставалась открытой, и со стороны выглядело, будто кнопка
  «Сохранить» не работает. Проверять этот паттерн в других формах: молчаливый
  `await api.post` без `catch` неотличим от сломанной кнопки.
- **Каталог заводится пачкой, а не по одному.** Склад → Материалы → «Ввести
  пачкой»: строка = материал, Tab между полями, Enter вниз, кусок таблицы
  вставляется из буфера (Ctrl+V) сразу в несколько строк и столбцов. Тип и
  производство принимаются названием, а не только выбором из списка. Пустое
  название собирается из полей, площадь листа — из размера, «листовой» —
  из наличия размера. Сохраняется всё или ничего: ошибки показываются с
  номерами строк, в базу не уходит ничего, пока они не исправлены.
- **Журнал движений пишет всё, включая продажи.** Типы: приход · продажа ·
  возврат · списание · инвентаризация; у записи есть ссылка на чек. Лента —
  Склад → «Движение» → «Журнал», по умолчанию показывает ВСЕ операции
  (продаж там больше всего — по записи на товарную строку чека), фильтры по
  типу/материалу/месяцу. Месяц по умолчанию не выбран: приходы вносят задним
  числом, и лента за текущий месяц прятала бы их в момент ввода.

- **Партию можно выбрать не только у рулона, но и у ЛИСТА (2026-08-27).**
  Выбранная пачка встаёт в списание первой, остальные идут за ней обычным FIFO;
  партия запоминается в строке чека даже без выбора — иначе возврат клал бы
  листы не в ту пачку. Материал строки РАБОТЫ при этом в базе не хранится
  (пробовали — ломается «Резка по станкам» при возврате материала и подпись в
  печати): при повторе заказа название берётся из товарной строки чека, как это
  делает отчёт по материалам.
- **Журнал склада показывает рулон МЕТРАМИ** (`InventoryLog.metres_changed`,
  миграция `warehouse/0009`). Метры пишутся В МОМЕНТ операции: восстановить их
  из площади задним числом нельзя — у каждой партии своя ширина. Записи до
  27.08 метров не помнят и остаются в кв.м, это не дефект.
- **Резка без единой услуги резки в каталоге больше не молчит.** Раньше окно
  считало работу (472), а в чек уходил один материал (427): строку работы не из
  чего было собрать. Теперь окно предупреждает, «Добавить» закрыто, итог
  честный. Первый симптом на чистой базе — именно этот.
- **Форма материала выводится из ЗАПОЛНЕННЫХ полей, отдельной колонки «форма»
  нет.** В сетке массового ввода: стоит ширина рулона — рулон, стоит размер
  листа — лист. Рулон без цены за пог.м и строка с обоими размерами разом
  отклоняются по существу.
- **Тёмная тема (2026-08-27).** Переключатель в шапке и на входе: система /
  светлая / тёмная, выбор в `localStorage` сильнее системного и применяется
  скриптом в `index.html` ДО отрисовки (иначе мигает белым). Цвета — только
  токенами в `:root`; **печатные формы намеренно не участвуют** (`.print-sheet`
  всегда белый с чёрным текстом — документ уходит на бумагу). Свой `add_header`
  и свой цвет в `location`/классе отменяют наследование — при новых правилах
  проверять обе темы.

## Где мы сейчас (2026-08-27, вечер)

- `main` = `952fd99`, запушено, **прод на этом коде** (пересобран 27.08,
  проверено открытием chpucenter.com: тёмная тема на месте). История линейная.
  При обновлении сервера `git pull` когда-то упирался в локальный коммит
  «исправление 1» — решено `git reset --hard origin/main`; впредь на сервере
  не коммитить.
- **Сессия 27.08: прогон ТЕСТИРОВАНИЯ.md с нуля на чистой базе** (услуги
  кнопкой, каталог пачкой из tsv, разделы 1–10, 12–13) + правки по находкам.
  Что сделано с 18.08 — одиннадцать коммитов, коротко:
  - кнопка «Новая услуга» и правка названия услуги в «Ценах и услугах»;
  - **техкарта услуги** правится там же («Настроить расход»): расходник, норма,
    режим. Механизм списания работал с самого начала, но строку заводили только
    через Django-админку — то есть никогда;
  - фильтр каталога по форме (штучный/лист/рулон); каталог грузился первой
    страницей из 25 и обрывался — исправлено;
  - **рулоны в «Ввести пачкой»**: группа колонок «Рулон» (ширина, цена за
    пог.м) В КОНЦЕ таблицы — порядок колонок это порядок столбцов при вставке
    из Excel, новый столбец в середине сдвинул бы цены в готовых таблицах;
  - карточка материала: цена больше не переписывает набранное (ввод 3400
    превращался в 3,01), убраны поля, которых у формы не бывает (артикул,
    единица у листа/рулона, размер листа у рулона), закупка рулона — за пог.м;
  - касса: резка по контуру у рулона; **выбор партии у листа** (как у рулона);
    защита от молчаливой потери работы, когда услуги резки нет в каталоге
    (окно показывало 472, в чек уходило 427);
  - журнал склада показывает рулон **метрами** (`InventoryLog.metres_changed`);
  - **деньги: закуп — оборот, а не расход** (см. ниже), инвестиции — свой блок;
  - дозаказ у админа, перенос даты накладной из окна документа;
  - **тёмная тема** (переключатель: система/светлая/тёмная);
  - закрыт голый `POST /api/sales/receipts/` — чек без позиций с «принятой»
    суммой клал деньги в кассу мимо кассы;
  - **бэкапы прода** (`deploy/backup.sh`, cron в 3 ночи) и **HSTS в nginx**.
- **Прод пересоздан начисто 18.08:** только
  4 учётки (`admin/admin12345`, складовщики `Лазер/Laser31.07.2026` и
  `Чпу/Operator31.07.2026` — по станкам, бухгалтер `Бухгалтер/Buhgalter31.07.2026`),
  **ни материалов, ни услуг** (даже «Резка лазером» из миграции `services/0003`
  удалена), справочники — из миграций. `seed` НЕ гонять: он заведёт
  `storekeeper`/`accountant` и каталог-заготовку. С этого момента записи на проде
  считать боевыми.
- **Локальная база (27.08) — не чистая:** на ней прогнали ТЕСТИРОВАНИЕ.md, там
  каталог из `номенклатура-для-вставки.tsv`, клиент «Тестов Клиент», чеки
  №1–20, партии и записи кассы. Копия «до прогона» — в scratchpad сессии 27.08
  (`db-before-testrun-2026-08-27.sqlite3`), может исчезнуть. Нужна чистая —
  удалить `db.sqlite3`, `migrate`, **`createcachetable`** (иначе логин отдаёт
  500: таблицу кеша `migrate` не создаёт, на проде это делает entrypoint) и
  завести 4 учётки руками; `seed` не гонять.
- Тесты: 834 зелёных за ~8 с (MD5-хешер под `manage.py test`).
- Аудит «где система врёт про деньги и остатки» закрыт по пунктам 1–3, 6–11,
  13, 14, 16–18 (перечень выше). Не делали: п. 12 (сдачу выдаёт только админ,
  зачёт по умолчанию включён — отложено пользователем), п. 15 («одна сторона» —
  решение заказчика).

## Ближайшее, что стоит сделать

**Хвост по проду (одна команда, не сделано):**
- **Cron бэкапов не записан.** Скрипт установлен и разово отработал 27.08
  (`/root/backups/db-2026-08-27.sql.gz`), но расписания у root нет:
  `echo '0 3 * * * /usr/local/bin/chpu-backup >> /var/log/chpu-backup.log 2>&1' | sudo crontab -`,
  проверить `sudo crontab -l`. Пока этого нет — копия ровно одна.
  Копии лежат на том же сервере, что и база: раз в неделю увозить `scp`-ом.
- **Услуги на проде** так и не подтверждены (пусто на 27.08). Теперь заводятся
  кнопкой «Новая услуга» в «Ценах и услугах» — Django-админка больше не нужна.

**Решения, которых ждём от заказчика (не начинать без запроса):**
- **Партии для штучных** (шт/кг/л): партий и FIFO у них нет вовсе — новая
  закупочная переоценивает весь старый остаток. Для саморезов неважно, для
  дорогой смолы может быть. Работа немаленькая: приход, списание, возврат,
  инвентаризация, отчёты.
- **Артикул**: убран из карточки материала (заказчик пишет его в названии), но
  остался в базе, поиске и сетке массового ввода. Убирать ли совсем.
- п. 12 аудита: сдачу выдаёт только админ (`give-change`), зачёт сдачи включён
  по умолчанию — складовщик, вписавший 1000 за 979 и отдавший 21 из ящика,
  оставляет «сдачу 21», и следующий заказ клиента дешевле на неё.
- Оплата при дозаказе: доплата — долг, но принять её может только админ;
  складовщику, взявшему деньги на месте, записать их нечем.

**Известные мелочи (не горит):**
- Онлайн-счёт + дозаказ: `_settle` ставит `amount_paid = total_price` целиком —
  подтверждение старого счёта зачло бы доплату как оплаченную. Онлайн на проде
  — заглушка.
- `SupplyLine.declared_length`: пара «заявлено / принято» у рулона есть только в
  модалке «Поступление» (админ); складовщик принимает накладной без неё.
- `Roll.width` хранит 2 знака, `Material.roll_width` — 3: ширина 1.265 в
  партии станет 1.26/1.27.
- Печать и телеграм-уведомления в прогоне 27.08 **не проверялись** (печать
  снимается только через предпросмотр браузера, бот не настроен).

ОБРЕЗКИ у рулона (18.08). Полосу 0.5 м от рулона шириной 0.9 отрезают на всю
ширину — иначе никак, рулон режут поперёк. Клиент забирает 0.5, а 0.4 остаётся
в цехе и обычно идёт в мусор. Деньги берутся за полную ширину, и это ПРАВИЛЬНО:
материал потрачен весь. Но цифры «сколько я подарил» не было нигде — обрезок
растворялся в списании.

Считается так: в кассе у рулона есть НЕОБЯЗАТЕЛЬНОЕ поле «ширина изделия»
(`TransactionItem.used_width`). Заполнили меньше ширины рулона — система знает
отход: `offcut_area = (roll_width − used_width) × длина`, где `roll_width` —
ширина ТОЙ ПАРТИИ, из которой резали (`TransactionItem.roll_width`; карточка —
только если партии у строки нет; до 18.08 брали карточку — аудит п. 10), а
`offcut_cost` — по цене той же партии (`item.roll.cost_per_sqm`, иначе последняя
закупочная). Не заполнили — считаем, что ушло всё, и ничего не выдумываем:
пустое поле не должно превращаться в «отхода нет», это разные утверждения.

Обрезок НЕ добавочный расход: он уже сидит в себестоимости строки, потому что
списали полную ширину. В финотчёте он стоит отдельной строкой «Из них в
обрезок» под себестоимостью проданного — как ответ на «сколько подарили», а не
как вычитание. Строка появляется, только когда ширину изделия называли: «0» при
незаполненном поле означало бы «отхода нет», а на деле его просто не считали.
Цена и склад от заполнения поля не меняются — проверено тестами
(`sales/tests_offcuts.py`, 11 шт.).

Отложено осознанно, не делать без запроса: деловой остаток как отдельная
сущность (`Remnant`) — он ДОБАВИТ ручного ввода на каждый рез, что против
главного требования (сама ЦИФРА обрезка у рулона уже считается, см. выше);
документ инвентаризации по ЛИСТОВЫМ материалам (у рулонов он есть — акт
промера); штрихкоды (для листов на стеллаже сканер бесполезен). Поставщик с
приходной накладной уже сделан (2026-08-14).

Остальные открытые вопросы — `HANDOVER.md`, раздел 10.

## Рабочий стиль с пользователем (соблюдать!)

- Тепло, без оборонительности, **прозрачно**. Если пользователь путается —
  это вина системы, НЕ его (так и говорить).
- **Не заваливать стенами текста.** Коротко, по делу, по-человечески.
- Показывать результат как ЖИВОЙ апп: поднимать превью и проверять фичу в
  браузере/через API, а не описывать словами, что должно получиться.
- **Бизнес-решения — за пользователем** (как считать деньги, что входит в
  резку, что показывать). Спрашивать, а не додумывать: несколько правок в
  этой сессии как раз меняли ранее принятые решения.
- **Не коммитить и не пушить без явной просьбы.** Коммиты по-русски, без
  упоминаний Anthropic/Claude.
- SSH-доступа к серверу у ассистента нет — деплой выполняет пользователь.
  На сервере оболочка **fish**: многострочные `manage.py shell <<'EOF'` — только
  из `bash`. Команды давать готовыми блоками с ожидаемым выводом.

## Как возобновить

Прочитать этот файл (статус, блок «Аудит», «Ближайшее»), затем HANDOVER.md
по нужному разделу — и действовать по запросу в чате. Автопамять
(`~/.claude/projects/.../memory/`) подхватывается сама: правила денег
(`money-rules`), прод и учётки (`deployment-target`, `repo-state-2026-07`),
стиль коммитов. Учётки локальной и боевой базы — выше в «Где мы сейчас»; в
README/HANDOVER таблицы «из seed» — это то, что заводит `seed`, а не то, что
стоит сейчас.

Что проверять командами, а не верить этому файлу: число тестов
(`manage.py test`), содержимое локальной базы (после 27.08 она с данными
прогона) и состояние прода — код там ровно тот, что запушен и пересобран
последним `docker compose … up -d --build`.
