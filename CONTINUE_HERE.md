# ЧПУ Система — продолжаем работу (handoff для нового окна)

> Это инструкция «для себя» в новой сессии. Цель пользователя: **довести систему до идеала, максимальная ПРОСТОТА и понятность**, не урезая функционал. Прочитай также файл памяти (он автозагружается): `~/.claude/projects/-Users-ashimovace-Desktop-projects-Typography/memory/cloude-project.md` — там полные тех-детали по каждой фиче.

## Контекст за 30 секунд
- **Что это:** Django 6.0 + DRF + JWT (бэк) и React 18 + Vite + i18next (фронт, `./frontend`). Система склада/продаж/финансов для ЧПУ-мастерской (резка акрил/форекс/алюкобонд и т.п.).
- **Название:** только **«ЧПУ Система»**. НЕ называть «Cloude»/«Claude» (раздражает пользователя).
- **Кто пользователь:** заказчик, НЕ технарь, легко «заваливается» от сложности, очень ценит простоту и дизайн. Уже был расстроен от не-в-стиле черновиков — всё строить в дизайн-системе апа (персик `#ffc592`, терракота `#b9692a`, Inter, бумажный фон, готовые компоненты `.card/.stat/.badge/DataTable/Modal/UIProvider`).

## Как запустить и проверять (важно!)
- **Серверы поднимать через Bash `run_in_background: true`** (НЕ через `&` — отваливаются; НЕ через Claude-Preview — его песочница не может поднять сервер, файловый sandbox запрещает):
  - Django: `.venv/bin/python manage.py runserver 127.0.0.1:8000 > /tmp/django.log 2>&1`
  - Vite: `npm --prefix /Users/ashimovace/Desktop/projects/Typography/frontend run dev > /tmp/vite.log 2>&1`
- **ВАЖНО про порт:** 5173 часто занят чужим dev-сервером → наш Vite сам встаёт на **5174**. Запускать `PORT=5174 npm --prefix /Users/ashimovace/Desktop/projects/Typography/frontend run dev > /tmp/vite.log 2>&1`, открывать `open http://localhost:5174/`. Живость: `curl ... http://localhost:5174/` (200) и `... -X POST http://127.0.0.1:8000/api/token/` (400 = жив). Ждать старт без foreground-sleep: `curl --retry 15 --retry-connrefused --retry-delay 1`.
- **Браузерную проверку Я ДЕЛАЮ САМ через Claude-Preview MCP** (открытие сессии 2026-07-01, старая заметка «не могу скриншотить» УСТАРЕЛА): `preview_start` поднимает СВОЙ Vite и проксирует `/api` на уже запущенный Django:8000 → доступны `preview_screenshot` / `preview_eval` (DOM-клики, скриншоты, чтение состояния). Логин в preview: `fetch('/api/token/'…)` → `localStorage.setItem('userToken', access)` + `user`. Для React-инпутов — нативный сеттер value + dispatch `input`. (Сам СЕРВЕР preview поднять не может — Django и основной Vite всё равно через Bash `run_in_background`.)
- **Креды:** `admin/admin12345`, `storekeeper/store12345`. Клиент — вход по телефону + **свой пароль** (задаёт при первом входе; сброс — персоналом из карточки клиента). Демо `+996700999002` (E2E ОСОО) — пароль сброшен, задаст заново.
- **Тесты:** `.venv/bin/python manage.py test` — **198/198 зелёных**. После правок sale_service/цен — `... test sales`.
- **Фронт-проверка:** vite собрался без ошибок (`tail /tmp/vite.log | grep -i error`, игнорить `useAuth ... Fast Refresh` — это безобидный варн). JSON-локали валидировать `python3 -c "import json;json.load(open(...))"`.
- **i18n:** 3 языка `frontend/src/i18n/locales/{ru,ky,en}.json`. RU — основной. Ключи добавлять маленьким python-скриптом (`json.load` → merge → `json.dump(..., ensure_ascii=False, indent=2)` + `\n`). Пропущенные en/ky падают на RU (fallbackLng).

## Что уже сделано (детали — в памяти)
1. Бренд → «ЧПУ Система» (3 языка). 2. Роли: склад/инвентаризация/списание → только админ (бэк `IsAdmin`, `403`). 3. Долг: `Receipt.amount_paid`+`stock_deducted`+property `debt`; предоплата в кассе; колонки/карточки долга. 4. Портал клиента: вход по телефону (`clients/customer.py`, отдельный JWT-scope, изоляция стафф↔клиент = 401), страница «Мои заказы» + кнопка «Как оплатить долг?». 5. Приложение `finance`: финотчёт + «Покупки/Инвестиции». 6. Разбивка резки по материалам. 7. Сортировка клиентов по имени. 8. Ручной погонный метр в резке.
- **Юзабилити-пасс** (субагенты-персоны «подросток» и «владелец» — ОЧЕНЬ помогли, пользователь оценил): упрощены подписи на 3 языках; добавлен экран **«Главная»** (`pages/admin/Home.jsx`, route `/admin` — 3 числа + крупные кнопки + «продажи делает сотрудник»); ADMIN_NAV переставлен; единые названия.
- **Резка — модель (решение заказчика): ЛЮБОЙ рез = работа.** «Весь лист» = материал; «Отрезать кусок» = ВСЕГДА материал(площадь) + работа реза(пог.м). Галочки «Резка» больше нет.
- **Разгрузка навигации (одобрено):** меню админа разбито на секции **«Каждый день»** (Главная·Чеки·Клиенты·Склад·Финансовый отчёт) и **«Реже»** (Покупки·Цены и услуги·Заявки на рефералов). `Layout.jsx` рендерит группы, `App.jsx` ADMIN_NAV = массив `{section, items}`.
- **«Склад» + «Приём товара» слиты (одобрено, 9→8 пунктов):** `pages/admin/Stock.jsx` — вкладки «Материалы» (Catalog) / «Движение» (Supply), оба с пропом `embedded`. `/admin/supply` редиректит на `?tab=movement`. (Открытый вопрос: вложенные 2 ряда вкладок vs плоский 1 ряд — пользователь пока оставил вложенный.)
- **Оптовая цена листа (одобрено, 28/28 тестов):** `Material.wholesale_price`+`wholesale_min_qty` (мигр. 0007), метод `piece_price_for_qty`. Опт ТОЛЬКО на целые листы, цена за лист, минимум свой у материала (умолч. 2). В кассе цена сама переключается на оптовую при кол-ве ≥ минимума (+бейдж «Опт»). Задаётся в Каталоге → блок «Продажа листом». Демо в БД: «Акрил 3мм» опт 3200 от 3 листов.
- **Дедуп цен материала (одобрено):** цены материала правились в Складе И в «Ценах и услуги» — убрал дубль. Теперь цены материала ТОЛЬКО в Складе (карточка материала, дополнил цена/кв.м + ставка реза); «Цены и услуги» = только работа (% мастера + ставки услуг). Принцип: материал+цена рядом, работа+ставка отдельно.
- **Редизайн модалки материала (одобрено):** была «длинная и непонятная» → переверстал: секции с заголовками («Цена и запас», «Продажа листом»), поля в 2 колонки, понятные подписи (`NumField`/`SectionLabel` в Catalog.jsx).

## СДЕЛАНО В ЭТОЙ СЕССИИ (2026-07-01) — самое свежее, детали в памяти
Всё одобрено заказчиком и проверено в браузере; **198 тестов зелёных**. По порядку:
1. **Edge-тесты + 8 багфиксов** (`sales/tests_edge_01..11.py`): falsy-ноль override, NaN/Infinity в pay, добор долга по частично-возвращённому, возврат неоплаченного онлайна, FIFO-restore рулонов, финотчёт-тест. Сошлись с серверным коммитом (pull) один-в-один.
2. **Django 6.0 в requirements** (коммит 81d1637). ⚠️ Локально ПОКА Python 3.9.6 + Django 4.2.30 (апп работает). **НЕ запускать `pip install -r requirements.txt`** — Django 6 не встанет на 3.9; заказчик решил уходить на Python 3.12+ (отдельная миграция, не начата). НЕ откатывать requirements.
3. **Финотчёт:** убран блок «Материалы» (транспорт входит в цену закупки); «Отчёт»+«Покупки» на ОДНОЙ странице (без вкладок); **посуточный график прибыли** (`DailyProfitChart`, постоянные расходы размазаны по дням месяца, стрелки ‹ ›).
4. **«Главная» админа = КАССА** (`/admin` → `<Checkout/>`; пункт меню «Продажа»); «Обзор» (аналитика) перенесён в меню «Реже». Отменяет прежнее «продажи делает сотрудник» — теперь и админ продаёт. `Home.jsx` осиротел.
5. **Индикация склада в кассе:** карточка краснеет + бейдж «Нет в наличии»/«Мало» + остаток (≈листы). **Продажа нулевого остатка ЗАПРЕЩЕНА** (кнопка disabled).
6. **Чеки:** сортировка **должники вверху** + **клик по заголовкам** (Долг/Итого/Дата, `DataTable` sort); **откат оплаты** (`unpay` — отменить ошибочно принятую оплату); долг сортируется на бэке (аннотация `_debt`, пагинация).
7. **Приём товара → на карточку материала** (кнопка «Поступление», `ReceiveStockModal`); в «Движении» остались только Инвентаризация + Списание.
8. **Флаг «Рулонный материал» → «Листовой / рулонный (по кв.м)»** (был бред: листовой акрил под «рулонным»). Только тексты, модель не трогали.
9. **«Весь лист» + резка по пог.м:** целый лист тоже можно резать (галочка в PIECE-режиме). Бэк: материал-строка по площади только `if area>0`.
10. **Клиентский портал ПОД ПАРОЛЕМ** (`Client.portal_password` хеш, мигр. `clients/0004`): первый вход — клиент задаёт пароль, дальше вводит; сброс персоналом (`reset-password`). Отменяет «вход по телефону без пароля».
11. **Округление цен ВВЕРХ до целого сома** (`TransactionItem.line_total` = ceil; фронт `ceilSom`). Строки, итог, погонометр, ценники — целые. Ставки/площадь/% — нет.

## Рабочий стиль с пользователем (соблюдать!)
- Тепло, без оборонительности, **прозрачно**. Если он путается — это вина системы, НЕ его (так и говорить).
- **Не заваливать стенами текста.** Коротко, по делу, с примерами «по-человечески».
- Показывать прогресс как ЖИВОЙ апп, не как сторонние демо/картинки.
- **Юзабилити-субагенты с персоной** (подросток/владелец, читают фронт+локали, отыгрывают растерянного юзера) — мощный инструмент, запускать при сомнениях в понятности. Фоном (`run_in_background`).
- Бизнес-решения (как считать деньги, что включает резка и т.п.) — спрашивать пользователя (`AskUserQuestion`), это его домен.

## Пакет правок заказчика (2026-07-01 доп.) — СДЕЛАНО 16 из 18, проверено в браузере, 198 тестов
Детали — в памяти (`cloude-project.md`, абзац «Пакет правок заказчика»). Кратко:
1. Оплата **+MBank +DemirBank** (как наличные). 2. **Человеческий №заказа** вместо UUID (order_number, старые пронумерованы). 3. **Кто оформил** = кассир+роль в чеках. 4. **Зарплата** — ручное поле в Финансах (авто-4% мастера убран). 5. **Инвестиции** (Оборудование+Улучшение цеха) вынесены из прибыли → блок «Вложения». 6. **Примечания** к коммуналке/прочим. 7. **Покупки** — редактирование категории/комментария. 8. **Обзор** — разбивка расходов + фильтр дат + экспорт CSV. 9. **Фильтр по материалам** в отчёте резки/продаж. 10. **Материалы на исходе** — список в Обзоре. 11. Установки — уже убраны (проверено). 12. **Заказы клиента** в его карточке. 13. Навигация складовщика: Продажа↑ Склад↓.
- **ОТЛОЖЕНО (заказчик, следующая задача):** реферальный бонус — **% от заказов приведённого клиента** (выплата/учёт в расходах). Механику проговорить перед реализацией.
- **ОТМЕНЕНО:** «код для входа клиента» — заказчик забыл удалить пункт, пароль-портал остаётся.
- Не коммитил (ждём просьбы заказчика). Финансовый пароль для preview: `finance123`.

## Следующий шаг (на чём остановились 2026-07-01)
- Пакет из 16 правок СДЕЛАН и проверен. Следующее по желанию заказчика — **реферальный бонус (% от заказов)**.
- **Возможные доработки (не начато, по желанию заказчика):**
  - Миграция на **Python 3.12+ / Django 6.0** (requirements уже на 6.0, но venv на 3.9/4.2): проверить deprecations 4.2→6.0, modeltranslation, прогнать 198 тестов.
  - Портал: «показать пароль» (глазик), восстановление по **SMS-коду** (сейчас сброс — только персоналом).
  - Финотчёт: cutting-разбивка считает raw `quantity*price_per_item` (не `line_total`) → копеечное расхождение с округлением; заменить на line_total при желании.
  - Прод-деплой (gunicorn+nginx+сборка фронта); настоящая онлайн-оплата долга (ключей FreedomPay нет, mock).
  - **Памятка-картинка** о системе для маркетолога (предложил, не делал).
- **Открытый вопрос (спросить заказчика):** `Receipt.debt` = 0 для `PARTIALLY_REFUNDED`, хотя `pay`/`unpay` умеют считать остаток — показывать ли долг по частично-возвращённым чекам в UI.
- **Git:** много НЕзакоммиченных изменений этой сессии (на диске, при новом окне не потеряются). Есть избыточный `stash@{0}` (старые фиксы 8 багов, дублируют серверные — можно `git stash drop`). Коммитить ТОЛЬКО по просьбе заказчика; на `main` — сначала ветка.
- **Общий вектор:** продолжать упрощать до «вау, просто».

## Как возобновить в новом окне
Скажи мне: «**продолжаем ЧПУ Систему, читай CONTINUE_HERE.md**». Память про проект подгрузится сама; этот файл — быстрый старт.
