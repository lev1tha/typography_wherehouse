# UI-ревизия глазами заказчика — 16.08.2026

Прогон по свежей базе (`rm db.sqlite3 && migrate && seed`), фронт `npm run dev`,
Playwright + Chromium 151, окно 1440×900 (мобильный проход — 500×900).
Логин `admin/admin12345`, складовщик `storekeeper/store12345`, финпароль
`finance123`. Скриншоты — в этой папке, номер в имени = шаг сценария.

На момент прогона ничего не чинилось; после отчёта по просьбе заказчика
починены пункты 1–10 (помечены ✅, скрины проверки `14x`–`20x-fix*`).
Ниже — только то, что видно в интерфейсе; бэкенд-тесты зелёные, и почти всё
найденное — рассогласование между экранами или между экраном и сервером.

Автоматическая проверка текста на всех скриншотах: `NaN`, `undefined`,
`[object Object]`, `null` и сырые ключи i18n (`nav.checkout` и т.п.) — **не
найдено ни разу**.

---

## 🔴 Критично

> ✅ **Пункты 1–10 починены (16–17.08.2026), 540/540 тестов зелёные.** Что и
> как — в конце каждого пункта («Починено») и в `CONTINUE_HERE.md`. Скрины
> проверки после правок — файлы `14x`–`20x-fix*`.

### 1. Печать любой формы даёт ПУСТОЙ лист

- Скрины: [24-A-supply-print-pdf.png](24-A-supply-print-pdf.png) (растр PDF —
  белая страница), [86-E-invoice-print.pdf](86-E-invoice-print.pdf) (1 КБ,
  пустой), [21-A-supply-print-media.png](21-A-supply-print-media.png) (страница
  в режиме `print`). Для сравнения предпросмотр —
  [20-A-supply-print-preview.png](20-A-supply-print-preview.png),
  [84-E-print-invoice-r2.png](84-E-print-invoice-r2.png).
- Воспроизведение: Склад → Приходы → Открыть → Печать → в окне печати
  «Сохранить как PDF» (или `page.pdf()` в Playwright, что то же самое). То же
  для Чеки → Печать → любая из трёх форм, и для акта сверки.
- Причина (видна из CSS): в `@media print` правило `.shell { display: none
  !important }` (`frontend/src/index.css:1664`), а окно печати
  `.modal-backdrop.print-host` рендерится ВНУТРИ `.shell` (в `<Outlet/>`), не
  через портал. Проверка в print-media: `.shell` display none, `.print-sheet`
  внутри `.shell`, размер 0×0.
- Затрагивает: товарный чек, накладную, счёт, акт сверки, приходную накладную —
  всё, ради чего сделаны реквизиты организации.
- ✅ **Починено:** окно печати выносится порталом в `<body>`
  (`components/PrintHost.jsx`), в `@media print` спрятан весь `#root` (у него
  `min-height: 100vh`, и пустой корень уводил лист на вторую страницу). Проверка
  PDF-ом всех пяти форм — по одной A4-странице с одним листом:
  [141-fix1-supply-print-pdf.png](141-fix1-supply-print-pdf.png),
  [144-fix1-invoice-print-pdf.png](144-fix1-invoice-print-pdf.png),
  `142/143/146/147-fix1-*.pdf`.

### 2. Касса считает заказ на 1 сом дешевле сервера → «Вся сумма» оставляет долг

- Скрины: [31-B-cut-side-filled.png](31-B-cut-side-filled.png) (касса: площадь
  0.553, материал 830, итого **978**),
  [33-B-order1-receipt-modal.png](33-B-order1-receipt-modal.png) (сервер:
  0.554 кв.м, 831, итого **979**, статус «Ожидает оплаты»),
  [37-B-receipts-list.png](37-B-receipts-list.png) (плитка «Долг 1 сом», кнопка
  «Оплатить» у только что полностью оплаченного чека),
  [40-B-print-check-r1.png](40-B-print-check-r1.png) («Оплачено 978 · Долг 1»).
- Воспроизведение: Продажа → «Акрил 3мм» (1500 сом/кв.м, рез 120 сом/пог.м) →
  режим «Одна сторона», ширина 0.45, длина 1.23 → Добавить → «Вся сумма» →
  Оформить.
- Руками: 0.45 × 1.23 = 0.5535 кв.м × 1500 = 830.25 → вверх **831**; работа
  1.23 × 120 = 147.6 → **148**; итого **979**. Сервер прав, касса ошибается.
- Причина: касса режет площадь до 3 знаков (`+(w*l).toFixed(3)` → JS даёт
  `0.553`, а не `0.554`) и уже её умножает на цену; сервер считает точную
  площадь. Плюс «Вся сумма» шлёт число, посчитанное кассой
  (`setPrepay(String(total))`), а не флаг `pay_full`, который у бэкенда есть
  специально для этого случая (`create_sale(pay_full=True)`, во фронте не
  используется ни разу). Любое расхождение округлений превращается в фантомный
  долг или сдачу.
- Побочно: та же деталь показывает разную площадь на разных экранах — 0.553 в
  кассе, 0.554 в чеке.
- ✅ **Починено:** площадь квантуется до 0.001 «половиной вверх» и на сервере
  (`sale_service._area`, до цены и списания), и в кассе (`utils/area.js`,
  целочисленно по десятичной записи); «Вся сумма» шлёт `pay_full`, поле
  показывает живой итог, ручной ввод снимает флаг; `quantity` в checkout
  принимает 3 знака (продажа площади 0.554 кв.м раньше падала с 400).
  Проверка: тот же кусок → касса 0.554 / 831 / **979**, чек **979**,
  «Оплачено», без долга ([150-fix2-cut-modal.png](150-fix2-cut-modal.png),
  [152-fix2-receipt-modal.png](152-fix2-receipt-modal.png),
  [154-fix2-receipts.png](154-fix2-receipts.png)). Тесты
  `sales/tests_rounding.py`.

### 3. Акт сверки не видит возвратов: в карточке долг 0, в акте — 226

- Скрины: [106-F-client-card-refunded-debt-order.png](106-F-client-card-refunded-debt-order.png)
  (карточка: Долг 0), [107-F-act-refunded-debt-order.png](107-F-act-refunded-debt-order.png)
  (акт: «Заказ №5 226,00 · Сальдо на конец периода 226,00 · Задолженность
  клиента 226,00 сом»), [105-F-refund-debt-order-detail.png](105-F-refund-debt-order-detail.png).
- Воспроизведение: заказ на 226 с ПУСТОЙ оплатой (долг) → /app/receipts →
  открыть → «Оформить возврат» → Клиенты → карточка → «Акт сверки».
- Тот же дефект мягче на оплаченном и возвращённом заказе
  ([104-F-act-gulnara-after-refund.png](104-F-act-gulnara-after-refund.png)):
  акт показывает «начислено 452 / оплачено 452 / сальдо 0», хотя заказ
  возвращён и 452 выданы клиенту из кассы
  ([97-F-cash-after-refund.png](97-F-cash-after-refund.png)) — сальдо
  совпадает случайно, движения в документе нет.
- Проверка «сальдо = долг на момент печати» на клиентах без возвратов прошла:
  1,00 = 1 сом ([66-C-act-aibek-debt.png](66-C-act-aibek-debt.png)), 0 = 0
  ([64-C-act-print-preview.png](64-C-act-print-preview.png)).
- ✅ **Починено:** в акте появились строки «Возврат по заказу №N» (погашение)
  и «Выдано деньгами по возврату №N» (начисление, не больше принятого),
  строки одного дня идут в порядке заказ → возврат → выдача → оплаты; на
  сервере возврат считается по `line_total` (вверх до сома), как итог чека.
  Проверка на четырёх клиентах — сальдо = долг карточки во всех:
  0 = 0 ([160-fix3-act-Нурлан.png](160-fix3-act-Нурлан.png), долг с возвратом),
  0 = 0 ([161-fix3-act-Гульнара.png](161-fix3-act-Гульнара.png), оплачено +
  возврат + выдача), 91 = 91 ([162-fix3-act-Айбек.png](162-fix3-act-Айбек.png)).

### 4. Финансы: две «Прибыли» на одном экране — −14 265 и +3 735

- Скрин: [72-D-finance-report.png](72-D-finance-report.png) — плитки сверху:
  «Расходы 18 000, Прибыль **−14 265**»; блок «По дням» тут же: «Расходы 0 сом,
  Прибыль **3 735**».
- Воспроизведение: одна накладная прихода на 18 000 (закуп считается
  автоматически «по приходам на склад»), несколько продаж, открыть Финансы за
  текущий месяц.
- Причина: график берёт расходы блока «Материалы» только из записей
  `ExpenseEntry` (`finance/views.py::DailyReportView`, ~строка 620), а плитки —
  из авторасчёта закупа по приходам. Пока закуп не вписан руками, график его не
  видит. Это ровно тот случай («две прибыли спорят»), который в
  `CONTINUE_HERE.md` считается закрытым.
- ✅ **Починено:** `purchases_from_stock_by_day` (тот же расчёт, что у строки
  «Закуп материала», по дням) прибавляется к дневным расходам при тех же
  условиях, что в плитках (вид не в архиве, «уменьшает прибыль»). Проверка:
  плитки «Выручка 6 162 · Расходы 18 000 · Прибыль −11 838» и график «Выручка
  6 162 · Расходы 18 000 · Прибыль −11 838»
  ([171-fix4-finance-daily-crop.png](171-fix4-finance-daily-crop.png)). Тесты
  `finance/tests_daily_purchases.py`, `CogsTests` переписаны на сверку с
  плитками. Осталась одна намеренная разница: в текущем месяце график не
  списывает долю постоянных расходов на будущие дни (плитки берут месяц
  целиком) — при заведённой аренде итоги разойдутся до конца месяца.

---

## 🟠 Существенно

### 5. Правка состава чека молча переносит заказ на 12:00 и пишет ложную запись в журнал действий

- Скрины: [100-F-after-edit-receipts.png](100-F-after-edit-receipts.png) (чек №2
  был «16.08.2026, 22:09», стал «16.08.2026, **12:00**»),
  [101-F-journal-after-edit.png](101-F-journal-after-edit.png) (продажа №2
  уехала в конец ленты — ниже поступлений, т.е. «продали раньше, чем
  приняли»), [102-F-audit-log.png](102-F-audit-log.png) («Правка чека 2:
  client, order_date, title» — ничего из этого не менялось).
- Воспроизведение: Чеки → Изменить → поменять только количество строки →
  Сохранить.
- Причина: `EditReceiptModal` всегда шлёт `PATCH {title, client, order_date}`,
  а бэкенд на любой `order_date` ставит `created_at = полдень` и двигает
  `happened_at` списаний. Само требование «в журнале ОДНА запись продажи с
  новым количеством» выполнено (−0.30 кв.м, без пары возврат+продажа).
- ✅ **Починено:** форма шлёт только изменённые поля; сервер при той же дате
  время не трогает и пишет в журнал только реально изменённые поля.
  Проверка: правка количества в №7 — время осталось 22:56, в журнале только
  «Правка состава чека 7: было → стало»
  ([180-fix5-audit-after-qty-edit.png](180-fix5-audit-after-qty-edit.png)).

### 6. Возврат: у админа кнопки нет вообще, возврата одной позиции нет нигде

- Скрины: [37-B-receipts-list.png](37-B-receipts-list.png) и
  [90-F-admin-edit-modal-r4.png](90-F-admin-edit-modal-r4.png) — действия по
  чеку у админа: Откат / Печать / Изменить / Удалить; «Возврат» отсутствует.
  Возврат нашёлся только в складском разделе `/app/receipts` → стрелка →
  «Оформить возврат» ([91-F-store-detail-r4.png](91-F-store-detail-r4.png),
  [92-F-refund-confirm.png](92-F-refund-confirm.png)) — целиком по чеку, ссылки
  на этот раздел из админ-меню нет (только руками в адресной строке).
- Сценарий «верни одну позицию» через интерфейс невыполним: бэкенд принимает
  `item_ids`, фронт всегда шлёт `{}`.
- Сам возврат целиком отработал верно: остаток 27,77 → 29,77
  ([95-F-journal-after-refund.png](95-F-journal-after-refund.png)), запись
  «Возврат +2.00», статус «Возвращено», касса −452
  ([97-F-cash-after-refund.png](97-F-cash-after-refund.png)).
- ✅ **Починено:** общая модалка `RefundModal` — кнопка «Возврат» в действиях
  чека у админа и «Оформить возврат» в складской детали; отмечаешь, что
  вернули (по умолчанию всё), уходит `item_ids`. Попутно выправлены два правила
  бэкенда, без которых частичный возврат врал бы деньгами: долг считается и у
  частично возвращённого чека (раньше после возврата одной строки остаток долга
  пропадал из карточки, плиток и списка), а деньгами возвращается ровно
  переплата относительно оставшихся позиций (раньше `min(возврат, оплачено)`
  при частичной оплате мог отдать больше принятого). Проверка: №7 (оплачено
  1000 из 1879), вернули строку на 900 → «Вернётся деньгами 21», касса −21,
  остаток 0.6 кв.м на складе, статус «Частичный возврат», долга нет
  ([182-fix6-refund-modal-one.png](182-fix6-refund-modal-one.png),
  [183-fix6-receipts-after-partial-refund.png](183-fix6-receipts-after-partial-refund.png),
  [185-fix6-store-refund-modal.png](185-fix6-store-refund-modal.png)). Тесты
  `sales/tests_partial_refund.py`.

### 7. Складовщик не может завести приход

- Скрин: [130-G-store-home.png](130-G-store-home.png) — его «Склад» — только
  карточки с остатком и ценой, ни «Приходов», ни «Поступления». Страница
  `pages/store/Supply.jsx` есть, но в `App.jsx` не подключена. API
  `SupplyViewSet` складовщику разрешён (`IsAuthenticated, IsNotAccountant`).
- Остальное по роли — как задумано: `/admin/finance|cash|dashboard` редиректят
  на `/app` ([131](131-G-store-finance-url.png), [132](132-G-store-cash-url.png),
  [133](133-G-store-dashboard-url.png)), API отвечает 403, в чеках нет
  себестоимости и маржи, нет поля даты заказа, печать накладной доступна
  ([138-G-store-print-waybill.png](138-G-store-print-waybill.png)).
- ✅ **Починено:** в меню складовщика появился «Приём товара» (`/app/supplies`)
  — тот же экран накладной, что у админа; отмена накладной ему по-прежнему не
  показывается. Проверка: складовщик завёл НК-002 (201), открыл, напечатал
  ([188-fix7-store-supplies-after.png](188-fix7-store-supplies-after.png),
  [189-fix7-store-supply-print.png](189-fix7-store-supply-print.png)).

### 8. Липкая шапка каждого модального окна перекрывает содержимое

- Скрины: [30-B-cut-modal-default.png](30-B-cut-modal-default.png) (вкладки
  режимов срезаны сверху без прокрутки),
  [11-A-new-material-filled.png](11-A-new-material-filled.png) и
  [12-A-new-material-filled-bottom.png](12-A-new-material-filled-bottom.png)
  (при прокрутке поля «Акрил / 3», «Бишкек» видны НАД шапкой),
  [17-A-supply-discrepancy.png](17-A-supply-discrepancy.png),
  [43-B-edit-receipt-r1.png](43-B-edit-receipt-r1.png),
  [60-C-client-card-debt.png](60-C-client-card-debt.png) — подпись первого
  поля наполовину под шапкой.
- Измерено: шапка стоит на 24px ниже верха окна и перекрывает первую строку на
  8px уже без прокрутки. Причина — `.modal-head { position: sticky; top: 0;
  margin: -24px -24px 16px }` (`index.css:1044`): у sticky-элемента
  отрицательный margin в offset не уходит.
- ✅ **Починено:** у `.modal` убран верхний отступ (`padding: 0 24px 24px`), у
  шапки — отрицательный вертикальный margin; sticky-шапка теперь стоит вплотную
  к верху окна и накрывает прокрученное. Измерено: зазор над шапкой 0, наложение
  на первую строку 0 ([190-fix8-cut-modal.png](190-fix8-cut-modal.png),
  [191-fix8-material-modal-scrolled.png](191-fix8-material-modal-scrolled.png),
  [200-fix8-mobile-modal.png](200-fix8-mobile-modal.png)).

### 9. Таблица «Чеки» не влезает в 1440px

- Скрины: [37-B-receipts-list.png](37-B-receipts-list.png) (колонка «СД…»
  обрезана, дальше пусто) vs [52-C-receipts-change-column.png](52-C-receipts-change-column.png)
  (после горизонтальной прокрутки: Сдача, Дата, Печать/Изменить/Удалить).
- Измерено: таблица 1653px при контейнере 1144px. Все действия по чеку — за
  правым краем, полосы прокрутки не видно, пока не начнёшь крутить.
- ✅ **Починено:** колонок стало 10 вместо 14 («кто оформил» под клиентом,
  способ оплаты под статусом, себестоимость под маржой, сдача в колонке долга,
  печать вместе с остальными действиями 2×2), плотный режим таблицы
  (`.table-wrap.dense`), итог тем же форматом, что остальные суммы; у
  прокручиваемых таблиц нарисована постоянная полоса прокрутки. Измерено:
  1440 → 1144/1144, без прокрутки; 1680 — без прокрутки; 1280 — 39px прокрутки
  с видимой полосой ([196-fix9-receipts-1440-actions.png](196-fix9-receipts-1440-actions.png),
  [194-fix9-receipts-1280.png](194-fix9-receipts-1280.png), мобильные карточки
  [199-fix9-mobile-receipts-card.png](199-fix9-mobile-receipts-card.png)).

### 10. Цена листа расходится между Складом и Продажей

- Скрины: [04-empty-catalog.png](04-empty-catalog.png) — «Белый акрил: 1250
  сом/кв.м, **3725** сом/лист»; [01-empty-checkout.png](01-empty-checkout.png)
  — та же плитка в кассе «**3700** сом/лист». Для созданного «Акрил 3мм»: Склад
  **4465** ([13-A-catalog-after-create.png](13-A-catalog-after-create.png)),
  касса **4466** ([30-B-cut-modal-default.png](30-B-cut-modal-default.png)).
- Причина: Склад показывает `Math.round(price_per_sqm × piece_area)`
  (`Catalog.jsx::PriceCell`), не трогая поле `piece_price`, по которому реально
  продают; касса показывает `ceil(piece_price)`. Заказчик сверяет именно эту
  цифру с Excel.
- ✅ **Починено:** розничная строка «сом/лист» в Складе — это `piece_price` с
  тем же округлением вверх, что в кассе; нет цены за лист — нет и строки (в
  кассе продажа целиком тогда недоступна). Закупочная «за лист» по-прежнему
  считается из цены за кв.м (своей в базе нет). Проверка: Белый акрил
  3700/3700, Акрил 3мм 4466/4466, Форекс без строки листа в обоих
  ([197-fix10-catalog-prices.png](197-fix10-catalog-prices.png),
  [198-fix10-checkout-prices.png](198-fix10-checkout-prices.png)).

### 11. Окно «Правка чека»: итог меняется от одного открытия

- Скрин: [43-B-edit-receipt-r1.png](43-B-edit-receipt-r1.png) — открыли чек
  №1, ничего не трогали: «Итого ~~979~~ **980** сом».
- Причина: `Math.ceil(0.554 × 1500)` в JS = `Math.ceil(831.0000000000001)` =
  832; в кассе для этого есть `ceilSom` с эпсилоном, здесь его нет
  (`EditReceiptModal.jsx:52`).

### 12. Обзор: цифры на одном экране не складываются

- Скрин: [73-D-dashboard.png](73-D-dashboard.png).
  - «Расходы по статьям»: строка «Себестоимость проданного 1 850 сом» стоит в
    списке, а итог «Расходы 18 000» её не включает — список читается как
    сумма, но ею не является.
  - «Продали материала на 3 142» + «Выручка: работа 592» = 3 734 ≠ «Выручка
    всего 3 735». Разбивка суммирует сырые `qty × price` (830.25 + 147.6…),
    чеки — построчно округлённые вверх. (На SQLite количество хранится как
    0.5535, на Postgres будет 0.554 — цифры чуть другие, но природа та же.)
  - «Покупки материала по клиентам» считает только ОПЛАЧЕННЫЕ чеки (сумма по
    таблице 2 312), а «Продали материала» — все (3 142); подписи об этом нет.
  - «Материалов на исходе: 16» — считает и позиции с нулём («Нет в наличии»),
    хотя Склад их «на исходе» сознательно не считает
    ([04-empty-catalog.png](04-empty-catalog.png)).

### 13. KY/EN: сырых ключей нет, но новые разделы целиком по-русски

- Сравнение словарей: в `ky.json` нет 102 ключей из `ru.json`, в `en.json` —
  108. Целиком отсутствуют `print.*` (52), `supplies.*` (17), `cash.*` (22),
  `period.*` (4), часть `finance.*` (реквизиты) и в EN — подсказки режимов реза
  `checkout.modeSideHint/CurveHint/SqmHint`, `sideAuto`, `perPieceShort`.
  i18next подставляет русский, поэтому «ключей» на экране нет, но:
  - EN, окно реза: заголовки по-английски, подсказка режима по-русски
    ([127-G-en-cut-modal.png](127-G-en-cut-modal.png)); «≈0 sht» вместо
    pcs/sheets.
  - EN/KY, печать: «SALES RECEIPT № 2 **ОТ** 16.08.2026», «ТОВАРДЫК ЧЕК № 2
    **ОТ** …», шапка таблицы «Наименование · Кол-во · Ед. · Цена · Сумма»,
    «Итого/Оплачено/Долг/Сумма прописью/Продавец» — по-русски
    ([128-G-en-print.png](128-G-en-print.png),
    [128-G-ky-print.png](128-G-ky-print.png)).
  - EN, Приходы: подсказка и плитки по-русски
    ([124-G-en-supplies.png](124-G-en-supplies.png)); Касса — вся по-русски
    ([126-G-en-cash.png](126-G-en-cash.png)); Финансы — реквизиты, закрытие
    периода по-русски ([125-G-en-finance.png](125-G-en-finance.png)).
  - В KY 78 значений совпадают с русскими (`Телефон`, `Статус`,
    `Администратор`, `Касса`…) — часть допустима как заимствования, но
    `roles.admin`, `nav.cash`, `receipts.status` стоит проверить с носителем.

---

## 🟡 Мелкое / косметика

14. **Карточка клиента после возврата** ничего не говорит о возврате: заказ №4
    «452 сом» без строк и без пометки, оплаты 452, LTV 0
    ([98-F-client-card-after-refund.png](98-F-client-card-after-refund.png)).
    В списке клиентов у неё «Заказов: —», в карточке «Заказов: 1»
    ([94-F-store-list-after-refund.png](94-F-store-list-after-refund.png) /
    [98](98-F-client-card-after-refund.png)).
15. **Поставщик в накладной**: если «+ добавить…» не удался (дубль имени),
    введённое имя остаётся в поле, а накладная проводится с поставщиком «—»
    ([17-A-supply-discrepancy.png](17-A-supply-discrepancy.png) →
    [19-A-supply-doc.png](19-A-supply-doc.png)). Текст ошибки сервера:
    «поставщик с таким название уже существует» (падеж).
16. **Модалка чека после оформления** не показывает ни оплачено, ни сдачу —
    только статус ([51-C-order3-receipt-modal.png](51-C-order3-receipt-modal.png)
    после [50-C-order3-pay3000-before-submit.png](50-C-order3-pay3000-before-submit.png),
    где сдача 1500 была видна). Строки чека там и в складской детали без единиц
    («Акрил 3мм × 0.554», «Резка букв × 1.230» —
    [45-B-store-receipt-detail-r1.png](45-B-store-receipt-detail-r1.png)); в
    печатной форме единицы есть.
17. **Форматы чисел вперемешку** в одной строке чеков: «1500.00 сом» (Итого),
    «1 500 сом» (Сдача), «447 сом» (себестоимость)
    ([52-C-receipts-change-column.png](52-C-receipts-change-column.png));
    критический остаток «2.00» ([04](04-empty-catalog.png)); журнал «Лист
    1.22×2.44 ×5.00 … 12000.00 сом» ([23-A-journal-after-supply.png](23-A-journal-after-supply.png)).
18. **Мобильный (500px)**: таблицы становятся карточками, бургер работает,
    горизонтального переполнения нет ни на одном экране
    ([111](111-G-m-receipts.png), [113](113-G-m-catalog.png),
    [115](115-G-m-cash.png), [117-G-m-burger-open.png](117-G-m-burger-open.png)).
    Мелочи: select «Тип» режет «Физ. лицо» → «Физ. лицс»
    ([110-G-m-checkout.png](110-G-m-checkout.png)); чек в кассе — в самом низу
    под всеми товарами, подсказка «нажмите на товар слева» (на телефоне —
    сверху); в Чеках пять плиток статистики столбиком занимают экран до
    фильтров ([111](111-G-m-receipts.png)).
19. **Плитки «Чеки»** на 1440 ложатся 4+1 («Сдача клиентам» одна во второй
    строке), фильтры «С/По» разъезжаются на две строки
    ([02-empty-receipts.png](02-empty-receipts.png)).
20. **Акт сверки**: оплаты идут в обратной хронологии (252 раньше 200,
    [64-C-act-print-preview.png](64-C-act-print-preview.png)); заказы — тоже
    новее сверху ([66](66-C-act-aibek-debt.png)).
21. **Приход материалов из seed** («Форекс 3мм» и др.): у них есть площадь листа
    (в каталоге показана цена за лист), но нет размера листа → в накладной с
    формой «Лист» ширина/высота не подставляются, и без ручного ввода приход
    отклоняется «не из чего посчитать количество»
    ([18-A-supplies-list.png](18-A-supplies-list.png) — тост при первой
    попытке).
22. **Счёт без банка**: вкладка «Счёт на оплату» выключена, объяснение — только
    во всплывающей подсказке при наведении; видимый текст ниже говорит про
    «шапку», не про банк ([42-B-print-invoice-r1-nobank.png](42-B-print-invoice-r1-nobank.png)).
23. **Дата в `<input type=date>`** показывается как `mm/dd/yyyy` — это локаль
    браузера (в headless — en-US), у заказчика будет `дд.мм.гггг`; не дефект,
    но в скриншотах бросается в глаза.

---

## ❌ Где цифры между экранами НЕ сошлись (сводка)

| # | Где | Что показано | Что должно быть / где иначе |
|---|-----|--------------|-----------------------------|
| 2 ✅ | Касса → чек №1 | касса 978 (0.553 кв.м, 830) | сервер/чек/печать 979 (0.554 кв.м, 831); долг 1 сом после «Вся сумма» — **починено** |
| 3 ✅ | Карточка клиента ↔ Акт сверки | долг 0 | сальдо 226 (акт не учитывает возврат) — **починено** |
| 4 ✅ | Финансы: плитки ↔ «По дням» | прибыль −14 265, расходы 18 000 | прибыль +3 735, расходы 0 — **починено** |
| 5 ✅ | Чеки/журнал склада после правки | заказ №2 22:09 | стал 12:00, продажа «раньше поступления» — **починено** |
| 10 ✅ | Склад ↔ Продажа | 3725 сом/лист; 4465 | 3700 сом/лист; 4466 — **починено** |
| 11 | Правка чека | итог 980 без изменений | 979 |
| 12 | Обзор | материал 3 142 + работа 592 = 3 734; клиенты 2 312; расходы-список с 1 850 | выручка 3 735; продано 3 142; итог 18 000 |
| 14 | Список клиентов ↔ карточка | Заказов «—» | Заказов 1 |

Всё остальное сошлось: выручка 3 735 = сумма чеков (979 + 804 + 1500 + 452);
касса 978 + 3000 + 804 = 4 782, после сдачи −1500 = 3 282, после возврата
+200 +252 −452 = без изменений ([53](53-C-cash-after-3000.png),
[56](56-C-cash-after-change.png), [97](97-F-cash-after-refund.png)); долг
452 → 252 → 0 в карточке, чеках и акте ([60](60-C-client-card-debt.png)–[64](64-C-act-print-preview.png));
остатки склада 14,884 → 13,09 → 13,03 сходятся с журналом; сумма системы
18 000 против «по бумаге» 18 500 подсвечена красным
([17](17-A-supply-discrepancy.png), [19](19-A-supply-doc.png)); стоимость склада
54 390 = сумма категорий.

---

## ✅ Что проверено и работает как задумано

- Обычный рез: в работу идёт **длина одной стороны** (1.23 пог.м × 120 = 148),
  фигурный — введённые вручную 3.7 пог.м; в чеке две строки — материал (кв.м ×
  цена) и работа (пог.м × ставка); округление вверх до сома построчно
  ([31](31-B-cut-side-filled.png), [34](34-B-cut-curve-filled.png),
  [40](40-B-print-check-r1.png), [82](82-E-print-check-r2.png)).
- Сдача: 3000 за заказ 1500 → «Сдача 1500» в кассе и в Чеках, приход в кассу
  3000 (принесённое), «Выдать» → расход 1500 ([50](50-C-order3-pay3000-before-submit.png)–[56](56-C-cash-after-change.png)).
- Пустая оплата → весь заказ в долг; «Погасить долг» частично/полностью из
  карточки; акт при отсутствии возвратов сходится с долгом.
- Закрытие периода: понятный отказ на заказ задним числом, не белый экран
  ([76-D-backdated-order-refused.png](76-D-backdated-order-refused.png)); журнал
  действий пишет закрытие/открытие.
- Финпароль: неверный — «Неверный пароль» ([71](71-D-finance-gate-wrong.png)).
- Счёт заблокирован без банка и открывается после заполнения реквизитов
  ([84](84-E-print-invoice-r2.png)); сумма прописью верна на 452 / 804 / 979 /
  1 500 / 18 000.
- Пустые состояния всех разделов ([01](01-empty-checkout.png)–[07](07-referrals.png)).
- Роль складовщика ограничена и в интерфейсе, и в API (см. п. 7).

---

## Как это гонялось

Скрипты Playwright лежат в scratchpad сессии (`…/scratchpad/pw/*.js`, общий
`lib.js` — логин, скриншот с автосканом текста, API-обёртка). Каждый шаг делал
`page.screenshot({fullPage})`, файл открывался и просматривался глазами;
числа сверялись с ответами API (`/api/sales/receipts/`, `/api/audit/dashboard/`)
и с ручным расчётом.
